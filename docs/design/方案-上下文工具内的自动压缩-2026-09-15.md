# 方案 · 把「自动压缩」收进上下文工具（v2 · 已按批注修订）

> 状态：**阶段 0–3 已完成**（阶段 2 见 §10、阶段 3 见 §11）；阶段 4 待排期，
> 开工契约见 §12（按《上下文管理方案修改提点（阶段 4 前）》修订，2026-09-16）；
> 用户新提供的《长上下文状态化压缩参考方案》已对齐并排成任务，见 §13（2026-09-17）。
> v1（2026-09-15 初稿）→ 你留了批注 → v2 按批注修订 → 你确认了接口三处改动 → 本文再同步定稿与阶段 1 结果；
> 你的批注原文完整保留在[附录 B](#附录-b--审阅批注原文2026-09-15)，未做删改。
> 修订摘要见 §0，技术事实核实见 §3。

## 0. 修订摘要（批注 → 采纳情况）

| # | 批注要点 | 采纳 | 落到哪 |
|---|---|---|---|
| 1 | 阶段 1 不要提前改主值为工作集语义（底层还是 pi 原生压缩，UI 会与真实行为不一致） | ✅ 全盘采纳 | §4、§6 阶段 1 |
| 2 | 阶段 2 优先走 RPC 事件，不必写 extension | ✅ 采纳，且**已验证可行** | §3.2、§6 阶段 2 |
| 3 | 阶段 3 不能只理解成"关掉 auto-compaction + 调 compact()"；要拆成 Policy（决策）+ 执行层 | ✅ 采纳 | §3.3、§6 阶段 3 |
| 4 | 70/85/100 是**工作集预算**百分比，不是物理窗口百分比；UI 不要突出"100%" | ✅ 采纳 | §4、§5 |
| 5 | 工作集公式必须含输出预留与安全余量 | ✅ 采纳，给出可验算公式 | §5 |
| 6 | "历史已保留/已归档" ≠ "模型可回溯" | ✅ 采纳，文案分级 | §4、§7 |
| 7 | 真正做成多阶段后改名「自动管理上下文」/「立即整理」 | ✅ 采纳（按阶段 4 执行，阶段 1 不改名） | §7 |
| 8 | 重新分期：0 定义 → 1 UI 归位 → 2 可观测 → 3 接管工作集 → 4 多阶段与归档 | ✅ 采纳，并补了阶段 0 的接口草案 | §6、附录 A |

## 1. 问题（同 v1）

右栏现状：

```
┌ 上下文 ──────────────────────────┐
│ 上下文  9%              36k / 400k│   ← 主值是物理窗口占用
│ ▬▬▬▬▬▬▬▬▬▬▭────────────────────── │   ← 一条触发线记号
│ > 详情                           │
│ 自动压缩        [开关]  [压缩上下文]│   ← 独立一行，看起来像第二个工具
└──────────────────────────────────┘
```

| # | 问题 |
|---|---|
| 1 | **看起来像另一个工具**：自动压缩被「详情」折叠区隔在主值之外，与「上下文」没有视觉隶属关系 |
| 2 | **主值语义**：显示物理窗口占用；换到 1M 模型时用户会以为"还早"，而编码模式该守的是远低于窗口的工作集 |
| 3 | **详情混三类**：策略参数 / 能力开关 / 花销 |
| 4 | **表达不了"会发生什么"**：只有单点阈值，没有阶段、没有"历史在哪" |

## 2. 目标（同 v1）

让「上下文」一屏回答：① 离"该管理"还有多远 ② 下一步自动发生什么 ③ 被移出的历史在哪、能不能取回。

原则：**压缩是上下文的一种行为，不是一个并列的工具。**

## 3. 技术事实核实（v2 新增，决定方案可行性）

我核对过内置 pi 0.85.1 的扩展 API 与 RPC 协议，结论如下（都有代码依据）：

### 3.1 可观测性：**RPC 事件已经存在，且砚已经在收**

| 事件 | 字段（实测） | 砚现状 |
|---|---|---|
| `compaction_start` | — | 已处理（只标记 streaming + 刷新状态） |
| `compaction_end` | `reason`（如 `threshold`）、`status`（`completed` / `declined`）、`entryId`、`lane`、`runId`、`endedAt` | 已处理（只标记结束 + 刷新） |

pi 侧还存在 `compaction_delta`、`compaction_cost`、`compaction_declined`、`session_compact_failed`、`before_compaction`、`autocompact` 等名字，需在实际运行时确认哪些会经 RPC 转发（这不是阻塞项）。

**结论**：批注第 2 点成立 —— 阶段 2 的"正在压缩 / 已完成 / 被拒绝 / 原因"**不需要 extension**，只要把已到达的事件字段用起来。

### 3.2 执行层：pi 扩展提供两个真正能改上下文的钩子

| 钩子 | 语义（源码行为） | 用途 |
|---|---|---|
| `context(messages)` | 扩展收到**发给模型的完整消息数组**，可返回 `{ messages }` 替换它 | **Tool Sweep / Episode Fold 的执行层** |
| `session_before_compact` | 可返回 `{ cancel }` 取消，或返回 `{ compaction }` 用自己的结果替换 pi 的摘要 | **结构化 Coding State 摘要**（替换对话式总结） |
| `before_agent_start` | 可改系统提示（已在用：自主模式、语言、回答档位） | 注入当前任务契约等 |

**结论**：批注第 3 点成立 —— 多阶段管理是**可实现的**，而且执行层能落在 pi 扩展里，砚负责决策。这也说明 v1 把阶段 3 写成"关掉 auto-compaction + 调 `compact()`"确实不够：那只是"更早/更晚做一次完整压缩"。

### 3.4 职责边界（已定）

| 侧 | 模块 | 负责什么 |
|---|---|---|
| **砚（桌面端）** | `ContextPolicy` / `ArchiveStore` / `Settings` / `UI State` | **什么时候做什么**：算工作集预算、决定触发哪一阶段、保存归档元数据、把状态反映到 UI |
| **pi Extension** | `ContextTransformer` / `CompactionAdapter` / `RecallTool` | **具体怎么改变送给模型的上下文**：`context` 钩子做 tool sweep / episode fold / recall 注入，`session_before_compact` 接管结构化压缩 |

边界：**原始 session/history 保持完整**，Extension 只改 active model context；不 fork pi core。

### 3.3 现有能力清单（砚已接）

`compact(customInstructions)`、`set_auto_compaction(enabled)`、`compactionInfo`（阈值/保留/预留）、模型 `contextWindow` 与 `maxOutputTokens` 能力表。

## 4. 目标信息架构（分两态）

### 阶段 1 态：**忠实显示 pi 当前真实状态**（不含未生效的工作集）

```
┌ 上下文 ─────────────────────────── 36k / 400k ─┐
│ 9%                              ╷ 触发线 384k    │   ← 主值仍是物理窗口
│ ▬▬▭──────────────────────────────────────────── │
│ 自动压缩  [开关]                       立即压缩   │   ← 归位：进上下文主体
│ 详情 ⌄                                          │
│   容量      窗口 400k · 保留最近 20k · 预留 16k   │
│   最近一次   阈值触发 · 已完成（或：未发生过）      │
│   本轮花费   $0.32                               │
└─────────────────────────────────────────────────┘
```

要点：**不出现**工作集数值、不出现三阶段记号、"详情"里加一行**最近一次压缩的事实**（来自 §3.1 的 `reason`/`status`）。

### 阶段 3 态：接管后切到工作集视角

```
┌ 上下文 ───────────────────────────── 36k / 240k ─┐
│ 15%   [清理]      [折叠]        [压缩]             │   ← 文字标记，不写 100%
│ ▬▬▭────────────────────────────────────────────── │
│ 下一步：清理旧工具输出（约 168k 时）                │
│ 自动压缩  [开关]                        立即压缩   │
│ 详情 ⌄  物理窗口 1M · 预留 32k · 余量 20k · 已归档 12 段 │
└──────────────────────────────────────────────────┘
```

三个记号写成「清理 / 折叠 / 压缩」，**百分比只放 hover 与详情**——避免"100% = 上下文满了"的误读（1M 模型下 100% 只等于物理窗口的 24%）。

## 5. 工作集与预算公式（v2 修订）

```
responseReserve = clamp(preferredReserve, 16_000, contextWindow × 0.25)   // preferred = 32k
safetyMargin    = max(8_000, contextWindow × 0.02)

workingSet      = min(
                    240_000,                                  // 编码模式天花板
                    contextWindow × 0.70,                     // 窗口比例
                    contextWindow - responseReserve - safetyMargin
                  )

triggers = { sweep: 0.70 × workingSet, fold: 0.85 × workingSet, compact: workingSet }
emergency = min(0.90 × contextWindow, contextWindow − responseReserve)
                                        // 物理兜底，防止策略自身出 bug 顶爆；
                                        // 但不能突破输出预留（§12.1 / D31）
```

验算（确认小窗口不会冲突）：

| 物理窗口 | 输出预留 | 安全余量 | 工作集 | 说明 |
|---|---|---|---|---|
| 64k | 16k | 8k | **40k** | 由 `窗口 - 预留 - 余量` 决定，不被 44.8k 卡住 |
| 128k | 32k | 8k | **88k** | 同上 |
| 256k | 32k | 8k | **179k** | 由窗口比例决定 |
| 1M | 32k | 20k | **240k** | 由绝对上限决定 |

## 6. 分期（v2 重排，采纳批注第 8 点）

| 阶段 | 交付物 | 验收 | 成本 |
|---|---|---|---|
| **0 · 定义** | `ContextPolicy` / `ContextOperation` / `ContextBudget` / `ArchiveEntry` 类型与事件映射（**已定稿**，见[附录 A](#附录-a--阶段-0-接口草案)） | 类型通过 typecheck；映射表覆盖 completed / declined / failed / manual / overflow | 0.5 天 · ✅ 定稿 |
| **1 · UI 归位** | 自动压缩移入上下文主体；详情分「容量 / 花费」两组；去掉重复的只读开关行；文案与图标收敛到 `ctx.*`。**主值保持物理窗口，不显示工作集** | 探针通过 + 视觉证据 `docs/design/preview/context-stage1-2026-09-15.png` | 1–2 h · ✅ 已完成 |
| **2 · 可观测** | 用 RPC 事件显示"正在压缩（原因）/ 完成 / 被拒绝 / 失败"；详情里"最近一次"一行落地。**不引入 extension** | 真实触发一次压缩（手动 + 阈值），断言 reason 与 status 正确显示 | 0.5–1 天 · ✅ 已完成（2026-09-16，见 §10） |
| **3 · 接管工作集** | 砚按 §5 计算工作集并决定何时调 `compact()`；此时才启用工作集 UI；保留物理兜底（后按 §12.1 修正为 `min(90% 窗口, 窗口 − 预留)`） | 真实模型（小窗 / 大窗各一）验证触发点与兜底；`set_auto_compaction(false)` 后行为可控 | 1–2 天 · ✅ 已完成（2026-09-16，见 §11） |
| **4 · 多阶段与归档** | 扩展内实现 `context` 钩子的 **Tool Sweep**（旧工具输出 → tombstone，确定性、默认不调模型）与 **Episode Fold**（已完成工作段折叠成 `EpisodeState`）；`session_before_compact` 产出**结构化 Coding State**；archive 元数据 + `recall` 工具（带独立预算） | **开工契约与 10 条验收见 §12**（原子上下文单元、两个 schema、禁止递归摘要、Recall 生命周期、降级路径）；真实长会话：上下文量下降、关键约束不丢、`ctx://` 引用可取回 | 3–5 天 |

**阶段 3 的边界（按批注）**：接管 = 决定"何时"做完整压缩 + 工作集可视化；**不等于**实现了 Tool Sweep / Episode Fold —— 那两项在阶段 4，靠 `context` 钩子做选择性变换。

## 7. 命名演进

| 阶段 | 名称 | 说明 |
|---|---|---|
| 1–3 | 自动压缩 / 立即压缩 | 此时确实只是 compaction |
| **4 起** | **自动管理上下文 / 立即整理** | 已经包含清理、折叠、压缩三类操作 |

文案分级（按批注第 6 点）：

- 有归档但未实现 recall → **"历史已保留 / 已归档"**
- recall 上线后 → **"模型可回溯"**

绝不提前写"可回溯"。

## 8. 明确不改的东西

- 不引入第二条滚动条（右栏只有一个滚动容器）。
- 阶段 1 不触碰 pi 的压缩行为。
- 不恢复"还差多少 token"的倒计时数字行（你此前的决定）；阶段 3 的「下一步」是**阶段名**，且可在详情里，不占主视区。

## 9. 已确认的决定（2026-09-15）

| # | 事项 | 你的决定 | 落地 |
|---|---|---|---|
| 1 | 阶段 0 接口形状 | 基本按草案，但 `kind` 用 `tool-sweep / episode-fold / compaction / recall` 四类，`idle` 不作为 operation（`currentOperation = null`）；`ArchiveEntry.recallable` 改**三态** `none / manual / agent` | ✅ 已写入附录 A |
| 2 | 阶段 1 | 开工，但严格限定为纯 UI 归位，**不显示尚未生效的工作集/三阶段线/“即将清理”** | ✅ 已完成（见 §6） |
| 3 | 阶段 4 执行层 | 放 pi Extension（`context` + `session_before_compact`），不 fork 内核；职责边界见 §3.4 | ✅ 已写入 §3.4 |

阶段 2 及以后开工前再单独确认。

---

## 10. 阶段 2 落地记录（2026-09-16）

### 10.1 接口落地：多了一个状态，并且拆成两份记录

| 决定 | 为什么 |
|---|---|
| `CompactionStatus` 加 **`cancelled`** | 实测中止给的是 `aborted: true`。把它归进 `failed` 会把“用户按了停止”说成“压缩崩了”；归进 `declined` 也错（那是上游主动不压）。 |
| `running` 与 `last` **分开两份**（`SessionState.compaction` / `.lastCompaction`） | 一条记录做不到：开始新一次压缩时要么擦掉上一次的结果（“正在压缩”时看不到上回压了多少），要么两个字段打架。分开后两者可以同时显示且永远不会矛盾。 |
| 多带 `beforeTokens` / `afterTokens` | `result.tokensBefore` / `estimatedTokensAfter` 本来就有。“上下文突然短了一截”的定量版本（`3.3k → 152`）；pi 不给就不显示。 |
| 自愈：pi 的 `get_state.isCompacting` 说“没在压”且本地还写着 running → 清掉 | 与 D19（文件树卡在“正在读取目录…”）同一类：少一条结束事件就会留下永久 spinner。 |
| 没有记录时**不渲染**“最近一次”这一行 | 从磁盘打开的历史会话可能早就压过，而砚不读会话文件里的 compaction 条目 —— 写“未发生过”是假陈述。 |

### 10.2 顺手发现的两个缺陷（已登记 D21 / D22）

它们都直接否定「界面上的数就是生效的数」这个前提，所以属于阶段 2 必须处理的东西：

- **D22**：用户级设置原来拼的是 `~/.pi/agent/settings.json`。pi 拿到的却是
  `PI_CODING_AGENT_DIR`（= 砚的 `PI_AGENT_DIR`）—— 便携版 / `YAN_PI_DIR` 下两份不是
  一个地方，界面会照另一份文件算触发线。已改为跟 `PI_AGENT_DIR`。
- **D21**：项目级 `.pi/settings.json` 只在 pi **信任该项目**时生效（该文件本身属于
  trust-requiring 资源），而 RPC 模式没有信任弹窗 —— 所以未在终端里 Trust 过的项目，
  其压缩设置会被 pi 整份忽略。实测三组对照：全局 → 第一轮就触发；只放项目级且无
  `trust.json` → 一轮都不触发；补上 `trust.json` → 又触发。已改为：**只把真会生效的那份
  当生效值**，并在详情里明说“项目的 .pi/settings.json 未生效”。

### 10.3 证据

| 项 | 证据 |
|---|---|
| 自动（阈值）压缩 | `npm run test:live -- compactionstatus`（cost 1）：真实压缩事件 `{reason:'threshold', result:{tokensBefore,estimatedTokensAfter}}` → 界面「已达阈值 · 已完成 · 3.3k → 152」 |
| 手动压缩**成功** | 同一场景：先关掉自动压缩（并断言行为真的被关住），再点「压缩上下文」→「手动 · 已完成」 |
| 手动压缩**失败不静默** | 同一场景：重复压缩 → pi 原文 `Compaction failed: Already compacted` 出现在详情里（不是一句“未知错误”） |
| reason × status 全表 | `declined` / `cancelled` / 未知原因（显示原文）/ 两行并存 → 探针第 4 节注入断言；分支逻辑单测 60+ 条 |
| 视觉 | `docs/design/preview/matrix-compaction-1440x900-100-{dark,light}-2026-09-16.png`（`npm run visual:matrix` 的 `compaction` 状态，带溢出与关键元素硬断言） |

### 10.4 剩余限制

- **`status` / `entryId` 那条 durable lane 形状没实测到**：归一化保留了分支，但没有真实
  场景能构造出来；目前所有实测事件都走 `result` / `errorMessage`。
- 会话文件里的 `compaction` 条目**仍未读**：切回旧会话时“最近一次”是空的（界面
  就不显示该行），压缩摘要也没进对话流。
- `declined` 只在 durable lane 出现，实测没碰到；“已跳过 + 解释行”目前只有注入证据。

---

## 附录 A · 阶段 0 接口草案

```ts
/** 上下文预算：由模型能力 + 策略参数推出 */
export interface ContextBudget {
  contextWindow: number          // 物理窗口（来自模型能力表）
  responseReserve: number        // 输出预留（§5）
  safetyMargin: number           // 安全余量（§5）
  workingSet: number             // 有效工作集上限
  triggers: { sweep: number; fold: number; compact: number }
  emergency: number              // min(0.90 × contextWindow, contextWindow − responseReserve)
}

/** 一次上下文变换：UI 状态与归档都从这里读。
 *  空闲态不是一种 operation —— 用 `currentOperation = null` 表示，不要加 `idle` 类型。 */
export interface ContextOperation {
  id: string
  kind: 'tool-sweep' | 'episode-fold' | 'compaction' | 'recall'
  status: 'running' | 'completed' | 'declined' | 'failed'
  reason: 'threshold' | 'manual' | 'overflow' | 'task-boundary'
  startedAt: number
  endedAt?: number
  beforeTokens: number
  afterTokens?: number
  archivedRefs: string[]         // 被移出 active context 的内容引用
  error?: string
}

/** 归档元数据（active context 之外的无损历史） */
export interface ArchiveEntry {
  ref: string                    // 例如 ctx://tool/8291
  kind: 'tool' | 'episode' | 'file' | 'diff'
  label: string
  createdAt: number
  tokens: number
  /**
   * 三态，而不是 boolean —— 「历史仍被保存」与「模型具备回溯能力」是两件事：
   *   none   不可恢复
   *   manual 仅砚/用户可恢复（历史已归档，模型取不回）
   *   agent  模型可通过 context_recall 主动取回
   * UI 只在 `agent` 时才能写「模型可回溯」。
   */
  recallable: 'none' | 'manual' | 'agent'
}

/** 策略参数（默认值取参考实践，可在设置里暴露） */
export interface ContextPolicy {
  enabled: boolean
  workingSetCap: number                                  // 240_000
  windowRatio: number                                    // 0.70
  recentTail: { target: number; max: number }             // 32_000 / 48_000（目标容量，切割按原子单元，§12.4）
  triggers: { sweep: number; fold: number; compact: number } // 0.70 / 0.85 / 1.00
}
```

事件映射（阶段 2 用 · **已按实测修正**，见 §10.1）：

| 来源 | 条件 | UI 状态 |
|---|---|---|
| `compaction_start` | — | 压缩中（带 `reason`；拿不到原因时不编） |
| `compaction_end` | 有 `status`（durable lane） | 按 `status`：`completed` / `declined` / `failed` |
| `compaction_end` | 无 `status`、`aborted=true` | 已取消（中断≠失败） |
| `compaction_end` | 无 `status`、有 `result` | 最近一次：已完成 · 原因 `reason` |
| `compaction_end` | 无 `status`、有 `errorMessage` | 失败 + pi 原文（不静默） |
| `compaction_end` | 三者都没有 | 失败（**不猜成功**） |
| `session_compact_failed` | — | 不过 RPC（只发给扩展），所以上面那条是唯一依据 |

---

## 附录 B · 审阅批注原文（2026-09-15）

> 以下为你在 v1 文档末尾留下的批注，原文保留，未做任何删改。

```text
回复：
整体方向我认可：把「自动压缩」收回到「上下文」内部，而不是继续作为一个并列工具，这个信息架构是对的；工作集、阶段状态、归档和回溯这几个概念也可以继续往下做。

不过实施顺序我建议调整一下，有几个点需要修改：

1. **阶段 1 不要先把主值改成 `36k / 240k` 的工作集语义。**
   现在阶段 1 定义为“纯 UI、不改策略”，但如果底层仍然使用 pi 原生 auto-compaction，那么 240k 工作集和 70%/85%/100% 阶段实际上还没有生效。UI 如果提前显示“168k 时清理”“240k 时压缩”，会出现界面行为与真实行为不一致。
   阶段 1 建议只完成 UI 归位，继续显示当前真实的物理上下文和 pi 压缩参数；等砚真正接管 Context Policy 后，再切换成工作集视角。

2. **阶段 2 的压缩状态可以优先直接走 RPC 事件，不一定需要 extension。**
   如果现有 RPC 已有 `compaction_start / compaction_end` 之类的生命周期事件，就可以直接用来显示“正在压缩 / 完成 / 失败 / manual / threshold / overflow”。Extension 留给真正需要修改上下文内容或压缩算法的阶段。

3. **阶段 3 不能只理解成“关闭 pi auto-compaction，然后按阈值调用 `compact(customInstructions)`”。**
   这种方式只能改变“什么时候执行一次完整 compaction”，并不能真正实现我们前面定义的：
   `70% Tool Sweep → 85% Episode Fold → 100% Structured Compaction`。
   Tool Sweep 和 Episode Fold 都属于选择性的 Context Transformation，需要单独的上下文管理逻辑。建议架构明确拆成：
   `砚 Context Policy Manager` 负责判断什么时候做什么，`pi extension/context layer` 负责真正执行 sweep / fold / compact。

4. **70% / 85% / 100% 必须明确是“工作集预算百分比”，不是模型物理窗口百分比。**
   例如 1M 模型、240k 工作集时，100% 实际只等于物理窗口的 24%。UI 上最好不要突出显示“100%”造成“上下文满了”的误解，可以直接把三个标记写成「清理 / 折叠 / 压缩」，百分比放 hover 或详情。

5. **工作集公式需要把输出预留和安全余量算进去。**
   不要只用：
   `min(contextWindow × 70%, 240k)`
   建议类似：
   `min(240k, contextWindow × 70%, contextWindow - responseReserve - safetyMargin)`
   否则在 64k/128k 这类小窗口模型上，固定 32k 输出预留可能和工作集预算冲突。

6. **“历史已保留”和“模型可回溯”需要区分。**
   在真正实现 `context_recall` 之前，可以说“历史已保留/已归档”，但不要直接写“可回溯”，因为这时只是 session 原始历史仍存在，并不代表模型能主动重新取回。等 Recall 工具上线后，再显示“模型可回溯”。

7. 后面真正做成 Tool Sweep / Fold / Recall 后，我建议把「自动压缩」改名成 **「自动管理上下文」**，因为那时它已经不只是 compaction 了；按钮也可以从「立即压缩」改成「立即整理」。

我建议重新分期：

* 阶段 0：先定义 `ContextPolicy / ContextOperation / workingSet / archive metadata`
* 阶段 1：UI 归位，仍忠实显示 pi 当前真实状态
* 阶段 2：接入 compaction 生命周期和可观测性
* 阶段 3：砚正式接管工作集阈值，此时再启用 `36k / 240k` 的工作集 UI
* 阶段 4：实现 Tool Sweep / Episode Fold / Structured Compact / Archive / Recall

所以这版我认为**设计思想可以通过，但实施顺序需要改**。最重要的两点是：不要让 UI 提前展示尚未生效的工作集策略；也不要把“主动调用 pi compact”误认为已经实现了完整的多阶段 Context Manager。
```

---

## 11. 阶段 3 落地记录（2026-09-16）

### 11.1 公式与参考值（§5 已实现）

`src/shared/context-policy.ts` 的 `contextBudget(窗口)`：

| 窗口 | 输出预留 | 安全余量 | 工作集 |
|---|---|---|---|
| 64k | 16k | 8k | **40k** |
| 128k | 32k | 8k | **88k** |
| 256k | 32k | 8k | **179.2k** |
| 1M | 32k | 20k | **240k** |

窗口小到装不下预留与余量时**返回 null**（策略不生效，交给 pi 原生压缩）——
不能给出一条 ≤ 0 的压缩线，否则每一轮都会触发。

兜底线在阶段 4 前置审核里被修正（**D31**）：`emergency = min(90% 窗口, 窗口 − 输出预留)`。
只按 90% 算时，64k 模型上兜底线 57.6k 而预留 16k —— 这条线自己就吃掉了留给回答的空间。
详见 §12.1。

### 11.2 三个拍板（实现时必须知道的）

1. **双层的「早压 / 兜底」**：砚只在工作集到线时压；pi 原生那条
   `窗口 − reserveTokens` **保持不动**（砚不写 pi 的设置文件）。实测同一个会话里
   pi 的线在 245760、砚的线在 1500（测试参数）——砚先动，pi 是它失灵时的兜底。
   另加一层 90% 窗口的硬兜底（`emergency`，不看「上膛」）。
2. **上膛与冷却**：用量回落到触发线 90% 以下才重新上膛；两次策略压缩之间至少隔 30 秒。
   没有这两条，压缩失败（例如 pi 回 `Already compacted`）会每一轮重试一次。
3. **发起方由砚盖章**：pi 对砚发起的 `compact()` 一律报 `reason: 'manual'`。
   实测**成功的压缩只有结束记录**（开始记录缺失，或被 `get_state` 的
   `isCompacting=false` 自愈清掉），所以盖章用「上一条记录的 `endedAt`」当基准
   来认出「本次调用产生的那条」，不能只认 `compaction_start`。

4. **触发时机只有回合结束**（`agent_settled`）。第一版把判定挂在“每次刷新用量”上，
   结果切到一个很大的旧会话（实测 276k tokens > 262k 窗口）的瞬间就压缩、实例变忙、
   「新对话」被拒 —— 用户只是想看一眼（D27）。回归网：`test:live -- contextswitchguard`。

### 11.3 界面上「未接管的阶段」怎么画

排期要求启用三标记，但 `tool-sweep` / `episode-fold` 属于阶段 4（要 pi 扩展的
`context` 钩子真的改上下文）。所以：三条刻度都画，**未接管的用虚线 + 说明**，
「下一步」只预报 `ContextPolicy.kinds` 里真的会执行的阶段；关掉「自动压缩」开关后
**整个工作集视角退回物理窗口**。

### 11.4 证据

| 项 | 证据 |
|---|---|
| 预算与界面同源 | `npm run test:live -- contextbudget`（cost 0）：参考值逐个断言、`SessionState.contextPolicy.budget` 与 `window.yan.contextBudget(窗口)` 相等、三条刻度位置（70/85/100% 各差 1px）、关掉开关后退回物理窗口 |
| 真实触发（工作集线） | `-- contexttakeover`（cost 1，`YAN_CONTEXT_POLICY` 把工作集压到 1500）：真实回合越线 → pi 真的压了 `5652 → 108`，记录 `triggeredBy=policy / policyStage=compact`，界面「工作集 · 已完成」（**不是「手动」**） |
| 真实触发（物理兜底） | `-- contextemergency`（cost 1，工作集抬到天上、`emergencyRatio=0.001`）：同一条路径命中 `emergency`，界面「物理兜底 · 已完成」 |
| 纯逻辑 | 单测 `test-context-policy.mjs` 85 条（另有 `test-compaction-status.mjs` 补的 4 条发起方断言）（公式验算表、四类不必触发的情形、上膛/冷却/兜底、env 覆盖解析、下一步阶段、文案） |
| 视觉 | `docs/design/preview/matrix-contextbudget-1440x900-100-{dark,light}-2026-09-16.png` |

### 11.5 剩余限制

- 策略参数只有代码默认值 + 测试用 `YAN_CONTEXT_POLICY` 覆盖，**设置面板里还不能改**。
- 策略**默认开启**：1M 窗口的模型也会在 240k 左右压缩（这是本阶段的设计意图，
  关掉即关「自动压缩」开关）。
- 清理 / 折叠仍是阶段 4；`recall` 仍未实现（界面不许写「可回溯」）。
- 连续多轮都压在工作集线上的长时会话没有压力测试（冷却 30s 是唯一护栏）——
  阶段 4 的验收第 10 条（§12.11）把这件事列为必做。
- 兜底线在 2026-09-16 的提点审核里被修正（D31，§12.1）；三阶段各自的上膛/冷却、
  可回收收益门槛等，都归到 §12 的阶段 4 契约里，阶段 3 不为它们提前写代码。

---

## 12. 阶段 4 前置设计约束（2026-09-16 · 按《上下文管理方案修改提点（阶段 4 前）》修订）

> 来源：`上下文管理方案修改提点（阶段 4 前）.md`（10 条修改 + 11 条验收建议）。
> 本节逐条审核：**采纳的写成阶段 4 的开工契约**；其中 1 条现在就是缺陷（已按 D31 修掉）；
> 有 1 条建议做了修订（**不照抄**），理由写在 §12.0。
> 阶段 4 的代码开工前先读完本节 —— §12.1–§12.10 是"怎么做"，§12.11 是"做到什么算完成"。

### 12.0 审核结论一览

| # | 提点 | 结论 | 落到哪 |
|---|---|---|---|
| 1 | 兜底公式要减输出预留 | ✅ 采纳（**现在就是缺陷**，D31 已修）｜更保守写法**不采纳** | §12.1 |
| 2 | 触发 = 水位 + 可回收收益 | ✅ 采纳，但**只管清理/折叠**；压缩不设收益门槛 | §12.2 |
| 3 | Rearm / Cooldown 推广到全部阶段 | ✅ 采纳（阶段 3 已验证这一套），状态抽成 per-stage runtime | §12.3 |
| 4 | `recentTail` 按原子上下文边界切 | ✅ 采纳，升为硬约束；32k 是目标不是切割位置 | §12.4 |
| 5 | Tool Sweep 走确定性压缩 | ✅ 采纳（与原方案同向，进一步明确"默认不调模型"） | §12.5 |
| 6 | Episode Fold 输出结构化状态 | ✅ 采纳，schema 定稿 | §12.6 |
| 7 | 禁止递归摘要 | ✅ 采纳，升为硬约束（含"怎么发现违反"的判据） | §12.7 |
| 8 | Structured Compaction 定 schema | ✅ 采纳，schema 定稿 | §12.8 |
| 9 | Recall 独立预算 | ✅ 采纳，另加"召回量必须计入触发判定" | §12.9 |
| 10 | 收益观测 | ✅ 采纳（阶段 2 已有 before/after）；低收益的处置做了修订 | §12.10 |
| — | 三阶段实际流程（水位→候选→执行/跳过） | ✅ 采纳，与 §12.2 合并 | §12.2 |
| — | 阶段 4 验收 10 条 | ✅ 全部采纳，补上取证方式与成本 | §12.11 |
| — | P0 / P1 / P2 分级 | ✅ 采纳，另指出 P0 里还有一项提点没提 | §12.12 |

**两处修订（审核意见）**

1. **兜底线不减安全余量**（提点修改 1 的"更保守"写法）。小窗口下工作集正好由
   `窗口 − 预留 − 余量` 决定，再减一次余量会让兜底线**等于**压缩线（64k 下都是 40k）；
   而"兜底不看是否上膛"是全系统唯一绕过上膛的路径 —— 两者相等就退化成
   "小窗口模型每轮都可以重试压缩"，正是修改 3 自己要防的事。取 `min(窗口×90%, 窗口−预留)` 后
   `emergency > triggers.compact` **恒成立**（余量 ≥ 8k > 0），兜底仍然是兜底。
2. **"低收益 → 不再频繁 Sweep"要限定为"本轮跳过 + 抬高门槛"，不是永久禁用**。
   工具输出是持续产生的：这次没有可回收的旧 tool result，下一轮可能就有一批。
   连续低收益该表达"现在不值得"，而不是"以后都不值得"。

**提点里没提、但阶段 4 必须一起定的两件事**

- **UI 文案随语义变**：水位线从"到线就做"变成"进入候选"之后，清理/折叠那两档
  **不能再写"下一步：清理旧工具输出"**（会被理解成承诺）。阶段 3 的「下一步」只报
  compaction，而它确实到线就压，所以现在不用改；等阶段 4 接上清理/折叠，这两档要改成
  「达到后会先看有没有可回收的旧工具输出」这类候选语义 —— "界面数字 ≠ 实际行为"
  是本项目最不能犯的错（D21/D22）。
- **降级路径**：每个变换都必须能在失败/超时后**退回 pi 原生行为**，不能把上下文留在半改状态
  （结构化压缩失败 → 交回 pi 自己的摘要；sweep 失败 → 一条消息都不动）。
  取证方式：探针里注入一个必然失败的结果，断言 active context 与原始 session 都没被破坏。

### 12.1 兜底公式（修改 1，已落地 · D31）

```
emergency = min(窗口 × 90%, 窗口 − 输出预留)      // 物理兜底不能突破输出预留
```

| 窗口 | 输出预留 | 工作集 | 压缩线 | 兜底线（新） | 兜底线（旧） | 旧值的问题 |
|---|---|---|---|---|---|---|
| 64k | 16k | 40k | 40k | **48k** | 57.6k | 只剩 6.4k 给回答（预留 16k） |
| 128k | 32k | 88k | 88k | **96k** | 115.2k | 只剩 12.8k 给回答 |
| 256k | 32k | 179.2k | 179.2k | **224k** | 230.4k | 越过预留 6.4k |
| 1M | 32k | 240k | 240k | 900k | 900k | 不变（90% 更小） |

三条必须对**所有**窗口成立的不变式（单测逐点扫过 1k–2048k 的窗口，见
`scripts/test-context-policy.mjs` 第 3b 节）：

1. `emergency ≤ 窗口 − 输出预留` —— 硬规则本身；
2. `emergency > triggers.compact` —— 兜底仍是兜底（不然会顶掉上膛与冷却）；
3. `emergency ≤ 窗口 × 90%` —— 用户调小 `emergencyRatio` 时仍然生效。

**与 pi 原生线的关系**：砚的预留（大窗口 32k）≥ pi 的 `reserveTokens`（默认 16.4k）时，
兜底线一定早于 pi 自己那条 `窗口 − reserveTokens` —— 砚先动、pi 最终兜底，层次不变。
反过来如果用户把 pi 的 `reserveTokens` 调得比砚的预留还大，pi 会先压：那是用户的显式选择，
砚不覆盖它（砚不写 pi 的设置文件）。

### 12.2 触发语义：水位决定"该关注"，可回收量决定"值不值得做"（修改 2）

```ts
interface ReclaimEstimate {
  reclaimableTokens: number   // 这次变换最多能腾出多少 token（上界估计）
  reclaimableRatio: number    // 相对当前用量
}
```

```ts
triggerReached && (reclaimableTokens >= minReclaimTokens || reclaimableRatio >= minReclaimRatio)
```

| 阶段 | 是否受收益门槛约束 | 为什么 |
|---|---|---|
| `tool-sweep` | ✅ 受 | 它是最便宜的一档，"收益太小就别动"代价很低 |
| `episode-fold` | ✅ 受 | 一次模型调用换 2k token 不值得 |
| `compaction` | ❌ **不受** | 它是"必须做点什么"的那条线：压不动也要压，否则接下来只剩兜底线与输出预留，没有第三个选项 |

估算从哪来（都不需要额外模型调用）：

- Sweep：候选集就是"已经不在最近 `recentTail` 窗口里的 tool result"，它们的 token 数**可以直接算**。
- Fold：候选集是"已关闭、且不在 recentTail 里的 Episode"，上界 = 这些 Episode 的 token 总和
  − 预计 `EpisodeState` 的 token；实际收益用 `beforeTokens − afterTokens` 校准（§12.10）。
- 估算只是**准入**，不是承诺：每次执行后都要记真实收益，下一轮的估算用实测值修正。

`minReclaimTokens` / `minReclaimRatio` 初值留待压力测试（§12.12 P2），但**必须有**：
没有它，170k 工作集只剩 2k 可回收时也会跑一次 sweep，界面与日志全是"整理了但没变化"。

### 12.3 每个阶段都有自己的运行状态（修改 3）

```ts
interface ContextStageRuntime {
  armed: boolean
  lastRunAt?: number
  cooldownUntil?: number
  lastBeforeTokens?: number
  lastAfterTokens?: number
  lastReclaimedTokens?: number
}

type ContextStageRuntimes = Record<'tool-sweep' | 'episode-fold' | 'compaction', ContextStageRuntime>
```

统一流程（阶段 4 照抄这一条，不要再各写一套）：

```
到线（候选）
  → armed?            否 → 记「待重新上膛」并退出
  → cooldown 到期?    否 → 退出
  → 估算可回收量       不足 → 跳过本轮（只记录：不改 armed、不重置冷却）
  → 执行
  → 记录 before/after/reclaimed、lastRunAt、cooldownUntil
  → disarm（armed = false）
  → 满足重新上膛条件（用量回落到 `线 × 0.9` 以下，或距上次超过重试窗口）后再 armed = true
```

**不许**"超过阈值 → 每轮都尝试执行"。阶段 3 已经踩过这个坑：`armed`/冷却/重试窗口
这三个参数就是为这件事存在的（`POLICY_COOLDOWN_MS` 30s、`POLICY_REARM_RATIO` 0.9、
`POLICY_REARM_MS` 5min，见 §11.2 与 D30）。阶段 4 落地时把这三条从"压缩专用"改成
**每阶段的默认值** —— `ContextPolicyState`（`src/shared/context-policy.ts`）就是这套机制
在单阶段（只有压缩）下的形态。

跳过（收益不足）与失败要分开记：跳过不改 `armed`（本来就还没动手），
失败要 disarm（否则会在同一轮里反复试）。

### 12.4 `recentTail` 与原子上下文单元（修改 4）

```ts
type ContextAtomicUnit =
  | { kind: 'user-turn'; entryId: string }
  | { kind: 'assistant-turn'; entryId: string }
  | { kind: 'tool-interaction'; callEntryId: string; resultEntryId: string }
  | { kind: 'agent-turn'; entryIds: string[] }   // user + assistant + 该轮所有 tool calls/results
```

- `recentTail.target = 32k`、`max = 48k` 是**容量目标**，不是切割位置：
  为了凑到 32k 而把一条 tool interaction 劈开是**禁止**的。
  实际尾部是 29k / 35k / 41k 都合法（语义完整优先于精确 token 数）。
- 最细粒度是 tool interaction：`assistant tool_call` 与它的 `tool_result` **不可分离**。
- 切割规则：从新往旧累加到 ≥ `target` 为止，然后对齐到 turn 边界；
  超过 `max` 也要继续往前退到最近的 turn 边界，而不是硬截。
- 硬约束（禁止产生）：
  - 留 `tool_call` 却清掉对应 `tool_result`；
  - `tool_result` 被截一半；
  - 系统提示 / 当前任务契约被当成可回收内容；
  - **（2026-09-17 补，N21-10）正在使用的 diff 不得删除**；
  - **（2026-09-17 补，N21-10）用户明确约束不得降级为普通历史信息**；
  - **（2026-09-17 补，N21-10）不得从 assistant 的 reasoning / output 中间切割**。
  唯一例外：Sweep 已经**显式**把该结果替换成 tombstone + `ctx://` 引用（§12.5）——
  那时剩下的不是"半条消息"，而是一条合法的、指向归档的占位。

**已落地为纯函数（N21-10，2026-09-17）**：`resources/pi-extensions/context-safety.js` +
单测 `scripts/test-context-safety.mjs`（42 条，进 `npm run test:unit`）。阶段 4 的扩展
在真正动 messages 之前调 `violations()`，非空就放弃这次压缩（宁可压不动，也不要发坏上下文）。
两条实现上的判断，别改回去：

- **"保护"分两档，强度不同**：
  · **硬留**（必须留在原始窗口里）：系统提示/契约、**正在使用的 diff**；
  · **可带走**（可以离开窗口，但必须进状态）：**用户约束**、未解决的错误。
  若把"用户约束"也当成硬留，cut 就永远退不到约束之后 —— 实际永远压不动；
  而"带走"这件事由 `carryOver()` 交出去、由 `violations()` 核对（忘了带会被抓）。
- **契约落在切片区里时，cut 选择"退到它之前"而不是"悄悄丢掉它"**：宁可这次不压，
  也不发一份少了任务契约的上下文。装配层（§12.5/N21-5）本来就会重新发出 system 与
  `<TASK_STATE>`，所以在真实链路上这种情况应当很少见；出现即说明该把契约提进状态。

与 pi 的关系：pi 自己也有 `compaction.keepRecentTokens`（保留最近多少 token），那是 pi 内部
压缩用的。阶段 4 的扩展自己决定 active context 的尾部，**不要两边都改**
（"砚不写 pi 的设置文件"这条界限不变）。

### 12.5 Tool Sweep 是确定性压缩，不是"再问一次模型"（修改 5）

默认**不调用模型**。旧工具结果 → 墓碑（tombstone）：

```text
[Archived tool result]
Tool: read
Target: src/foo.ts
Original tokens: 18,421
Ref: ctx://tool/8291
```

必要时保留**确定性元数据**（从结果里直接抄，不做推断）：退出码、错误行数、文件路径、
行区间、测试通过/失败数。

| 适合 Sweep | 不适合 |
|---|---|
| 旧文件整读（`read`）、目录列表 | 最近 `recentTail` 里的任何东西 |
| 长 stdout / build log / 测试日志 | 当前正在用的工具结果 |
| 网页全文、长 diff | 系统提示与任务契约 |
| 重复的 tool result、已失效的搜索结果 | 用户消息（任何情况下） |

原则：**快、便宜、可预测、信息损失边界清晰、原文可 Recall**。
sweep 必须**幂等**（同一份消息数组跑两次结果一致），否则界面上的"已归档 N 段"
会和真实 active context 对不上 —— 这一条可以直接单测。
真正需要语义理解的部分留给 Episode Fold。

### 12.6 Episode Fold 输出"工作状态"，不是"聊天摘要"（修改 6）

```ts
interface EpisodeState {
  id: string
  objective: string
  outcome: string
  decisions: { decision: string; reason?: string }[]   // 为什么这样做
  constraints: string[]                                // 什么不能改
  filesChanged: { path: string; summary?: string }[]
  failedAttempts: string[]                             // 试过但失败（含失败原因）
  unresolved: string[]
  importantRefs: string[]                              // ctx:// 引用
  sourceRange: { from: string; to: string }            // 指回**原始**条目 id（§12.7）
  tokensBefore: number
}
```

- 最重要的四栏是 `decisions` / `constraints` / `failedAttempts` / `unresolved` ——
  普通摘要把它们全丢掉，而它们正好是 coding agent 之后最容易重复踩坑的信息。
  字段可以为空，但**空必须在 schema 层面表示"确实没有"，而不是"忘了问"**：
  生成时按固定问题清单逐条填（要做什么 / 做成什么 / 为什么 / 什么不能改 /
  试过什么失败了 / 还差什么）。
- 关闭条件（何时可以 fold）：任务边界（用户换题目）、阶段完成、用户明确确认。
  没有关闭条件就不 fold —— 把"还在进行的活儿"折叠掉会直接误导下一步。
- 折叠结果要能被 Recall 取回原文（`ctx://episode/<id>`，对应 `ArchiveEntry.kind = 'episode'`）。

### 12.7 禁止递归摘要（修改 7，硬约束）

```
Episode 1 原始消息 → EpisodeState 1
Episode 2 原始消息 → EpisodeState 2
Episode 3 原始消息 → EpisodeState 3
active context = [EpisodeState 1..n] + recentTail        ← 允许
```

```
EpisodeState 1 + EpisodeState 2 → 新的自然语言摘要         ← 禁止
```

- **判据（怎么发现违反）**：`EpisodeState.sourceRange.from/to` 必须指回**原始条目**。
  某次 fold 的 sourceRange 落在另一份 EpisodeState 上，就是递归摘要 —— 单测直接断言。
- 结构化压缩需要全局状态时：**读结构化 EpisodeState + 仍然有效的原始状态 → 重建 CodingState**，
  而不是"摘要旧摘要"。
- 为什么：① 每层摘要都丢一点，且丢的偏偏是最不容易被模型注意到的约束类信息；
  ② 漂移不可追溯（无法回答"这条结论从哪来"）；③ 递归之后 `ctx://` 引用的可信度下降。

### 12.8 Structured Compaction 的 schema（修改 8）

`session_before_compact` 返回 `{ compaction }` 时用这份结构（替换 pi 自己的对话式摘要）：

```ts
interface CodingState {
  task: { objective: string; currentPhase: string }
  currentState: string[]
  decisions: string[]
  constraints: string[]
  files: { path: string; state: string }[]
  completed: string[]
  failedAttempts: string[]
  unresolved: string[]
  nextActions: string[]
  episodeRefs: string[]      // 被折叠的 EpisodeState id（不重新摘要它们）
  archiveRefs: string[]      // ctx:// 引用
}
```

它要回答的七个问题（写进生成清单里的固定提问）：

```
现在在做什么？已经做成什么？为什么这样做？什么不能改？
哪些方案试过但失败？还有什么没解决？下一步是什么？需要查历史时去哪里？
```

- 这是 **State Reconstruction**，不是 Conversation Summary：禁止"过去聊天主要讨论了……"这类句子。
- 与 §12.7 配合：`episodeRefs` 是引用不是内容 —— 不把 EpisodeState 再摘一遍。
- 降级：结构化生成失败/超时 → 交回 pi 自己的摘要（不 cancel），
  并在 UI 上标"结构化压缩失败，已退回 pi 原生摘要"，不静默。

### 12.9 Recall：独立预算与生命周期（修改 9）

```ts
interface RecallPolicy {
  maxTokensPerCall: number        // 单次召回上限
  maxActiveRecallTokens: number   // 同时驻留在 active context 的召回内容上限
  maxEntriesPerCall: number
  ttl: 'turn' | 'episode'         // 召回内容的保质期
}
```

反例（提点给的，必须用预算挡住）：

```
压缩后 240k → 100k
Recall A +50k → 150k
Recall B +60k → 210k
Recall C +70k → 280k        ← 绕过 ContextPolicy，直接顶穿工作集
```

- **召回是"读取历史"，不是"永久恢复历史"**：`ttl = 'turn'` 的召回内容在本次推理结束之后
  就可以再次被清理；只有新产生的**结论**值得留下，形式是"结论 + `ctx://` 引用"。
- 召回量**必须计入用量**（界面上的 tokens 会涨，这是对的）：于是它会推进下一次触发判定，
  而不是藏在预算之外。这条要写进 `context_recall` 工具自己的描述里。
- 三态 `ArchiveEntry.recallable` 不变：只有 `agent` 允许模型自己召回；`manual` 只能由
  用户/砚发起。每次召回留审计（时间、大小、发起方、命中的 ref）。
- 超预算的行为要明确：**拒绝并回一句可解释的话**（"这次召回 70k 超过单次上限 20k"），
  不要静默截断成半份内容 —— 半份历史比没有历史更危险。

### 12.10 每次变换都要有收益反馈（修改 10）

```ts
interface ContextOperationMetrics {
  beforeTokens: number
  afterTokens: number
  reclaimedTokens: number     // before - after
  reclaimedRatio: number      // reclaimed / before
  durationMs?: number
}
```

- 阶段 2 已经有 `beforeTokens` / `afterTokens`（`CompactionRun`），这一节把它升级成**反馈信号**。
- 反馈规则：
  - 连续 N 次（初值 3）`reclaimedRatio < 3%` → 该阶段**本轮跳过并抬高门槛**
    （不是永久禁用，见 §12.0 修订 2）；把机会让给下一档（Sweep 让给 Fold，Fold 让给 Compaction）。
  - 收益为**负**（`after > before`，例如折叠引入了 schema 开销却没压住）→ 记失败并回退本次变换。
- 观测落到可读的地方：详情里"最近一次"已经显示 `3.3k → 152`，阶段 4 追加阶段名与累计节省，
  并按阶段分开统计（一个阶段的坏结果不该污染另一个）。

### 12.11 阶段 4 验收（提点 10 条 + 取证方式）

| # | 验收项 | 怎么取证 |
|---|---|---|
| 1 | Sweep 后不存在 orphan tool_call / tool_result | 单测：构造含 tool interaction 的消息数组 → sweep → 断言成对性；探针：真实回合后读 active context 形状 |
| 2 | `recentTail` 永远按完整 Atomic Unit 切割 | 单测：多组 token 分布（含正好卡边界的），断言尾部边界落在 turn / tool-interaction 边界上 |
| 3 | 可回收量不足时 Sweep 正常跳过 | 单测：候选集为空或很小 → 断言 `skipped` 且 `armed` 不变；探针：注入"没有旧工具结果"的会话 |
| 4 | 三阶段各有独立 Rearm + Cooldown | 单测：三个 stage runtime 互不影响（一个 disarm 不影响另一个） |
| 5 | Episode Fold 输出固定 schema | 单测 schema 校验（缺字段 / 类型错 → 视为失败并回退）；探针用真实回合产出一次 |
| 6 | 已 Fold 的 Episode 不发生递归摘要漂移 | 单测：`sourceRange` 必须指回原始 entry（§12.7 判据）；连续两轮 fold 后断言 EpisodeState 只增不减 |
| 7 | Structured Compaction 保留 任务 / decisions / constraints / failed / unresolved / nextActions | 真实长会话（与第 10 条同一场景）：压缩后按这六类逐一断言非空 |
| 8 | Recall 受独立 token 预算限制 | 单测：单次超限被拒（可解释）、累计超 `maxActiveRecallTokens` 被拒；探针：真实召回一次并断言用量上涨 |
| 9 | Recall raw payload 不默认永久驻留 | 探针：召回 → 回合结束 → 断言 payload 被清理、只剩结论 + ref |
| 10 | 连续 20+ 长回合：维持在工作集附近、不每轮整理、不触碰 responseReserve、原始 session 不被破坏 | 真行为（cost 1）：用 `YAN_CONTEXT_POLICY` 把工作集压到几千 token（阶段 3 已验证这个手法可行）跑 20+ 回合；另跑一次真实长会话（不用测试参数）。断言：`tokens ≤ 工作集 × 1.1` 的回合占比、没有连续两回合触发同一阶段、`tokens < 窗口 − 预留` 恒成立、会话文件行数与原始条目哈希未变 |

### 12.12 优先级（提点分级 + 我们的补充）

**P0 · 阶段 4 开工前必须定（否则写完会返工）**

```text
Atomic Context Unit（含 recentTail 切割规则）      → §12.4
EpisodeState Schema                               → §12.6
CodingState Schema                                → §12.8
禁止递归摘要规则（含判据）                          → §12.7
Recall 生命周期（ttl + 预算 + 审计）                → §12.9
+ 降级路径：每阶段失败都退回 pi 原生行为             → §12.0 补充（提点没提）
```

**P1 · 阶段 4 实现时一起加**

```text
Reclaimability（含 minReclaimTokens / minReclaimRatio 的读取）
三阶段独立 Rearm / Cooldown（§12.3 的统一流程）
Recall Budget 的执行与拒绝文案
Operation Metrics + 低收益反馈
UI 候选语义文案：清理 / 折叠那两档不再承诺“会发生”
```

**P2 · 靠压力测试调参数（开工前不要拍死）**

```text
minReclaimTokens / minReclaimRatio
cooldown 时间、重试窗口
recentTail 的 32k / 48k
各阶段收益阈值（3% 那个数）
```

**阶段 3 已完成、阶段 4 不需要重做的**：预算公式与兜底线（§12.1，D31 已修）、
上膛 / 冷却 / 重试窗口这三个参数（只是从"压缩专用"变成每阶段的默认值）、
`YAN_CONTEXT_POLICY` 这条测试注入通道（阶段 4 的压力测试继续用它）。

---

## 13. 参考方案对齐：State-First Compaction / Trace-as-State（2026-09-17）

> 来源：用户提供的《砚 / pi 长上下文状态化压缩 · 智能体参考方案》。
> 原文**逐字**归档在 [参考-长上下文状态化压缩（外部方案·原文）](参考-长上下文状态化压缩（外部方案·原文）.md) ——
> 本节只做对齐、采纳与取舍，不改写原文。
> 它把两件事放在一起：**论文结论**（Trace as State：`[T, X, Q]` vs `[X, T, Q]`）、
> **pi 能力**（`context` / `session_before_compact`）、以及**工程建议**（阶段划分、配置默认值）。
> 我们逐条核对过 pi 0.85.1 的真实实现（见 §13.3），下面按"已实现 / §12 已定 / 新增任务 / 不采纳"分类。

### 13.0 一句话结论

参考方案没有推翻我们的架构，而是给 §12 补上**两件我们确实缺的东西**：
① **顺序**（Task State 必须排在历史**之前**，我们此前只定了"生成什么状态"，没定"状态放哪"）；
② **状态的生命周期**（版本化 / `active-superseded` / provenance，防旧决策污染与状态幻觉）。
另有两条我们**不照抄**：默认数值（我们已有的 240k / 0.7 是实测过的，见 §13.2）与
"State 就是压缩产物"的定位（我们把 Task State 与 EpisodeState 分成两层，见 §13.4）。

### 13.1 逐条对齐

| 参考方案 | 我们的现状 | 处置 |
|---|---|---|
| §1/§3 固定上下文顺序 `SYSTEM → TASK_STATE → HISTORY → RECENT_RAW → QUERY` | 未定（§12 只定义了状态内容与 `recentTail`，没说状态放在哪） | ✅ **采纳为新任务 N21-5**；这是本轮最大的缺口 |
| §2 论文：`[T, X, Q]` 优于 `[X, T, Q]`（条件先于数据） | 未做 | ✅ 采纳为 **N21-8**（Deep Context Mode）；论文结论我们**未复核**，只作为"值得试"的依据，不作为正确性证明 |
| §4 `TaskState` 数据模型 | §12.6 `EpisodeState`（每段折叠）+ §12.8 `CodingState`（全局重建） | ⚠️ **合并而非并列**：`CodingState` 就是我们的 Task State（阶段 4 契约），把参考方案里多出来的字段并进去（见 §13.4） |
| §5 State 与 Summary 的区别 | §12 已定"State Reconstruction 不是 Conversation Summary" | ✅ 一致（同一条原则的两种说法） |
| §6 五阶段（60% State Refresh / 75% Soft / 85% Hard / 93% Emergency） | 已实现三阶段 + 兜底（清理 70% / 折叠 85% / 压缩 100% + `emergency`） | ⚠️ **不照抄数值**（见 §13.2）；把"State Refresh"这一档**采纳**为 N21-6 的一部分（到 60% 就刷新状态，不必等到压缩） |
| §7 `effectiveContextWindow = min(window × 0.75, cap 300k)` | 已实现 `min(240k, 窗口 × 0.70, 窗口 − 预留 − 余量)`（有真实触发证据） | ⚠️ **保留我们的数**，把参考方案的 cap/ratio 作为可配置项（N21-7）；两者都允许模型级 override |
| §8 压缩产物 = Task State + Historical Summary + Recent Raw | §12.8 已有 `CodingState` + `archiveRefs`；`recentTail` 32k/48k 已定 | ✅ 采纳：`session_before_compact` 输出**两段**（`<TASK_STATE>` / `<HISTORICAL_CONTEXT>`）写进 N21-6 |
| §9 不推翻 pi 的 compaction，在它之上实现 | 已定（§11.2：pi 那条线保留为物理兜底） | ✅ 一致 |
| §10 `session_before_compact` 的用法与字段 | 未实现；**字段名已核实**（§13.3） | ✅ 采纳为 N21-6 |
| §11 用 `context` 钩子保证 State 前置 | 未实现；**返回值语义已核实**（§13.3） | ✅ 采纳为 N21-5 / N21-8 |
| §12 增量更新（Existing State + Delta，模型输出结构化 delta） | 未定 | ✅ 采纳：比"每轮重新生成"省 token 也更稳（进 N21-5） |
| §13/§14/§15 Deep Context Mode 与触发条件、Reasoning Effort 映射 | 未定 | ✅ 采纳为 N21-8（含触发条件与档位映射）；**默认关闭**，因为它是"多一次模型调用"的代价 |
| §16 压缩安全规则（不得从 tool call / reasoning / diff 中间切、不得摘要掉未解决错误…） | §12.4 已有原子单元与禁止切割；§12.6/§12.7 有 failedAttempts/unresolved 不得丢 | ✅ 一致；"当前 diff 不得删除""用户约束不得降级""不从 reasoning 中间切"三条已补进 §12.4 的硬约束，并**落地为纯函数 + 42 条单测**（N21-10，2026-09-17 关闭；反向验证：摘掉保护逻辑 6 条断言变红） |
| §17 模块划分（ContextManager 十个子模块） | 我们已有 `shared/context-policy.ts` + `main/context-policy.ts` + 计划中的扩展 | ⚠️ **部分采纳**：只落地需要的（State Store / Assembler / Metrics），不为对齐目录而拆十个模块 |
| §18 配置格式 | `ContextPolicy` 已有（enabled/workingSetCap/windowRatio/…） | ✅ 采纳其**形状**（ratio 全可配 + model override）为 N21-7；字段名沿用我们的（已进 IPC） |
| §19/§20 执行流程与 Assembler 伪代码 | 未实现 | ✅ 采纳为 N21-5 的实现依据（消息数组构造：system + 两个状态块 + recent + query） |
| §21 State 不是长期记忆（可覆盖/失效/删除 + `StateEntry.status`） | 未定 | ✅ 采纳进 N21-5 的 schema（`active` / `resolved` / `superseded`） |
| §22 防状态幻觉（`source` + `confidence` provenance） | 未定 | ✅ 采纳进 N21-5；并加一条我们的要求：**`source` 必须可校验**（`user`/`tool`/`file` 要有 entryId 或路径，模型自己写的只能是 `hypothesis`） |
| §23 Coding 专用状态（读过/改过哪些文件、跑过什么命令、测试结果…） | §12.8 `CodingState.files/completed/...` 部分覆盖 | ✅ 采纳 `commandsRun` / `testsRun` / `symbolsTouched` / `assumptions` 字段进 `CodingState` |
| §24 A/B 测试与指标（含 **Lost Constraint Rate**） | §12.11 有 10 条验收，但没有对照基准 | ✅ 采纳为 **N21-9**；`Lost Constraint Rate` 是我们之前没想的指标，直接补进 §12.11 |
| §25 五阶段实现顺序 | §12.12 有 P0/P1/P2 分级 | ✅ 两者合并：参考方案的 Phase 1→2→3→4→5 映射成 N21-5→6→7→8→9（顺序一致） |
| §27 "把 compaction 从生成聊天摘要升级成恢复任务状态" | §12 同一条原则 | ✅ 一致 |

### 13.2 数值冲突：为什么不照抄 300k / 0.75

| | 参考方案（§7/§18） | 我们（已实现，§11.1） |
|---|---|---|
| 有效窗口 | `min(窗口 × 0.75, cap 300k)` | `min(240k, 窗口 × 0.70, 窗口 − 预留 − 余量)` |
| 1M 模型 | 300k | 240k |
| 256k 模型 | 192k | 179.2k |
| 64k 模型 | 48k（可能吃掉输出预留） | 40k（**减去预留与余量**，避免与输出抢空间） |
| 阶段 | 5 档（60/75/85/93 + 100） | 3 档 + 兜底（70/85/100 + `min(90%, 窗口−预留)`） |

理由（三条，都是硬的）：
1. **小窗口下 `窗口 × 0.75` 会侵占输出预留** —— 这正是我们上一轮修掉的 D31（64k 窗口 48k > 40k）。
   参考方案的公式没有减 `responseReserve`，直接照抄会把 D31 重新引入。
2. 我们的数值**有真实触发证据**（`contexttakeover` 5652 → 108、`contextemergency`、四个窗口的参考值断言），
   换默认值等于把已验证的行为换成未验证的。
3. 数值本来就应该"工程策略 + 模型级 override"，参考方案自己也这么说（§7 末句）。

**处置**：数值进 N21-7 做成**可配 + 模型级 override**（含"想用 300k/0.75 就把这两个值改掉"），
默认保持现状；`State Refresh` 这一档（60%）**单独采纳**——它在我们的三阶段里没有对应物，
而且是纯收益（刷新状态比压缩便宜得多）。

### 13.3 pi 0.85.1 能力核实（本轮真查过代码，不是照抄文档）

| 能力 | 核实结果 | 依据 |
|---|---|---|
| `context` 钩子 | ✅ 存在且**返回值即生效**：`emitContext(messages)` 先 `structuredClone`，逐个扩展调用 `handler({type:'context', messages}, ctx)`，返回 `.messages` 就整体替换 | `resources/pi-runtime/dist/bundle/chunks/chunk-JVUZSMYM.js` 的 `emitContext` |
| `session_before_compact` | ✅ 存在，payload 字段与参考方案一致：`messagesToSummarize` / `turnPrefixMessages` / `previousSummary` / `fileOps` / `tokensBefore` / `firstKeptEntryId`（都在 bundle 里出现多次） | 同上（`grep -c` 逐个确认） |
| 扩展能不能自己调模型 | ✅ **能**（**2026-09-17 复核更正**，见 §16.1）：扩展上下文里有 `ctx.modelRegistry` 与 `ctx.model`，`ModelRegistry.complete(model, context, options)` 直接转发 `ModelRuntime.complete()` —— 一次**无工具**的模型调用不需要第二个进程。本表初版写的是「没有」，是只看了 `ctx.model` 这个名字、没有去查 `ModelRegistry` 的方法导致的误判 | `chunk-JVUZSMYM.js`：`ExtensionRunner.createContext()` 的 `get modelRegistry()` / `get model()`；`ModelRegistry=class{… complete(model,context,options){return this.runtime.complete(...)}}` |
| `agent_settled` 事件 | ✅ 对**扩展**分发：`async _emitAgentSettled(){ … await this._extensionRunner.emit({type:'agent_settled'}) … }` —— 「这一轮真的结束了」这个边界可以挂状态生成 | 同上 |
| 那 Deep Context Mode 怎么做 | ✅ **首选扩展内调用**：在 `agent_settled` 处理器（或 `context` 钩子）里 `ctx.modelRegistry.complete(ctx.model, { messages }, { tools: [] })` 生成 trace / working state，写进状态文件后被 `context` 钩子作为前置状态注入。砚侧独立 pi 进程（`main/title.ts` 那个模式）降为**备选**：只在需要多轮归纳、或要用另一个模型/额度跑归纳时才用 | 同上 + `src/main/title.ts`（备选先例） |
| KV cache 影响 | ⚠️ 未测 | N21-8 的前置调查项 |

### 13.4 两套状态的关系（必须一次说清，否则会并存两套）

```
Task State（= 我们的 CodingState，§12.8）   全局一份，随会话推进演化，**永远排在最前**
        ▲
        │ 由 EpisodeState 汇总 + 压缩时重建
EpisodeState（§12.6）                       每段一段，折叠后不重写（§12.7 禁止递归摘要）
```

- 参考方案的 `TaskState` ≈ 我们的 `CodingState`：**同一个东西**，合并字段，不新建第二张表。
- 我们**多**一层 `EpisodeState`：它是"证据层"，保证 Task State 可重建、可追溯（`sourceRange` 指回原始条目）。
- 参考方案的"增量 delta 更新"用在 **Task State** 上（便宜、稳定）；EpisodeState 不做增量（它是快照）。
- **删除/失效**：Task State 的条目可 `superseded`；EpisodeState 不删（归档保留，靠 §12.9 的 Recall 取回）。

### 13.5 需要用户拍板的三点（不定就不开工的部分）

1. **Deep Context Mode 是否在范围内**：它每次多一次模型调用（约等于一次归纳请求），
   换来的是长上下文条件下更好的推理质量。**2026-09-17 更正**：这次调用在扩展内完成
   （`ctx.modelRegistry.complete()`，§13.3），**不新增进程、不需要 sidecar**；需要拍板的只剩
   「愿不愿意多花一次无工具 completion 的额度与延迟」。默认关闭、按档位/阈值开启 —— 要不要做？
2. **默认数值**：保持我们已验证的 `240k / 0.7 / 三档`，还是切成参考方案的 `300k / 0.75 / 五档`？
   （我的建议：保持默认 + 暴露 override，`State Refresh` 那一档单独加。）
3. **状态文件的位置与保留**：`Task State` 落在 `YAN_DATA_DIR/context-state/<sessionId>.json`，
   会话删除时一并清理 —— 这属不属于"用户数据"，需不需要在设置里可见/可清空？

---

## 14. 阶段 4 · S1 落地记录：State / Archive 基础设施（2026-09-17）

> 阶段 4 是一个 3–5 天的大项，按用户指令**切成有边界的切片**推进，先做 S1。
> 本节只记 S1 定下来的东西（schema、文件位置、判据）与证据；S1 之后
> （Tool Sweep / Episode Fold / Structured Compaction / Recall 注入 / Deep Context）
> 尚未开工，不在本节宣称。

### 14.1 S1 做了什么、没做什么

| | 内容 |
|---|---|
| **做** | ① `TaskState` / `EpisodeState` / `ArchiveEntry` 的 schema 与校验（唯一真源 `src/shared/context-state.ts`）；② 文件信封：`schemaVersion` + `sessionId` + `sourceWatermark`；③ provenance 只认**原始 entry id**（`src/main/context-watermark.ts` 从会话 JSONL 读）；④ 原子写（临时文件 → 回读校验 → `rename`）；⑤ 损坏 / 版本不认识 / 引用已消失 → 安全丢弃；⑥ 会话删除时清派生状态（`sessions.ts` 的 `deleteSession`）；⑦ Deep Context artifact 的**注入闸门**（只预留，不实现第二次模型调用） |
| **不做** | 不改发给模型的消息；不实现 Tool Sweep / Episode Fold / Structured Compaction / Recall 注入；不改 `240k / 0.70 / 三阶段` 默认值；不新增设置项或普通 UI（派生状态对用户不可见）；不产生任何模型调用 |

### 14.2 定下来的 schema（后续切片照这个来，别另起一套）

- **Task State = §12.8 的 `CodingState`**（§13.4：同一个东西，代码里叫 `TaskState`，保留 `CodingState` 别名）。
  字段名沿用 §12.8，另加 §13.1 第 23 条的 `commandsRun` / `testsRun` / `symbolsTouched` / `assumptions` / `hypothesis`。
  与文档的唯一差别：**每一项从裸字符串升级成 `StateEntry`**
  （`text` + `status: active|resolved|superseded` + `updatedAt` + `source` provenance），
  这是 §13.1 第 21、22 条（状态要能失效、来源要可校验）的落地；`superseded` 必须写明被谁取代。
- **`StateProvenance`**：`kind ∈ user|tool|file|model` + `confidence ∈ observed|derived|hypothesis`。
  规则：`user` / `tool` 必须有 `entryId`；`file` 必须有 `path`；`model` 只能是 `hypothesis`。
  `entryId` 必须是原始 session 的 entry id —— **数字（数组下标）、token 偏移、`ctx://` 引用一律非法**。
- **`EpisodeState.sourceRange`**：`{ from, to }` 也是原始 entry id，且 `from` 必须不晚于 `to`。
  指向 `ctx://` → `archive-ref`，指向另一份 `EpisodeState` → `episode-ref`：
  这两个拒绝码**就是 §12.7「禁止递归摘要」的判据**（单测直接断言）。
- **`SourceWatermark`** = `{ entryCount, lastEntryId }`（来自会话 JSONL，不含文件头）。
  关系判定：`same`（一致）/ `older`（raw continue 变长，状态是较早的有效快照）/
  `diverged`（raw 被裁剪或换了一份历史 → **必须丢弃**）/ `unknown`。
- **`ArchiveEntry`**（元数据）：`ref` 形如 `ctx://<kind>/<id>` 且 kind 必须与 `kind` 一致；
  `recallable ∈ none|manual|agent`（UI 只在 `agent` 时才能写"模型可回溯"）；
  `sourceRange` + `watermark` 可追溯；`expiresAt` 是召回内容保质期；
  S1 的 `contentStored` 恒为 `false`（不落原文）。
- **Deep Context artifact**：`sessionId` + `sourceWatermark` + `createdAt` + `expiresAt` + `content`。
  `deepContextUsable()` 在注入前查四件事：会话一致、水位 **`same`**（不是 `older`）、未过期、内容非空。
  它不调模型、不改消息 —— 生成 trace 的 Pass 1 属于后续切片。

### 14.3 文件位置与生命周期

```
YAN_DATA_DIR/context-state/<sessionId>.json          状态 + Episode 列表
YAN_DATA_DIR/context-state/<sessionId>.archive.json  归档**元数据**（S1 不存原文）
```

- **原子写**：临时文件 → 回读 + 校验 → `rename`。校验不过**绝不 rename**，
  上一份好状态原样保留（单测：写非法状态后重新读出的是旧值）。
- **安全丢弃**：JSON 坏了 / `schemaVersion` 不认识 / 引用的原始条目已不在会话里
  → 删文件并返回原因。理由：active context 是可丢失派生物，原始 session 才是 source of truth；
  半份状态喂给模型比没有状态危险（§12.0 的降级路径）。
- **会话删除**：`deleteSession` 成功后按 sessionId 清状态 + 归档 + 崩溃残留的 `.tmp`。
  **撤销（恢复会话）不把状态找回来** —— 它是可重建的派生物，下一轮对话会重新生成。
- 状态文件默认**不在设置里露出**（§13.5 第 3 点尚未拍板）：S1 没有任何 UI / IPC 变更，
  renderer 碰不到 `YAN_DATA_DIR`。

### 14.4 证据（2026-09-17）

| 六栏 | 内容 |
|---|---|
| 实现 | `src/shared/context-state.ts`（schema/校验）、`src/main/context-state-store.ts`（原子写/丢弃/清理）、`src/main/context-watermark.ts`（原始条目身份）、`src/main/sessions.ts`（删除时清理） |
| 自动检查 | `npm run typecheck` 干净；`npm run test:unit` **1027/1027**（新增 `test-context-state.mjs` 76 条：schema 校验 / provenance / 递归摘要判据 / 水位 / 原子写 / 失败不覆盖 last-known-good / 损坏与版本不匹配丢弃 / 会话清理 / 真实 JSONL 水位）；`test:live -- contextstate`（cost 0，已进 `npm run check`） |
| 真实运行 | `test:live -- contextstate` 在真实 Electron 里删掉一条真实会话（8 → 7），退出后断言被删会话的状态文件已清理、其余 7 份一个没少、无 `.tmp` 残留；回归 `test:live -- trash`（删除 + 撤销恢复）与 `sessions`（切换/新建/连接）通过 |
| 视觉验收 | 不适用：S1 没有界面改动（派生状态对用户不可见，无新设置项） |
| 应用与包 | **未验证** —— 与其它 N21 项一样，共同缺口是 P0-8（用户要求放到最后）；`out/` 构建成功 |
| 剩余限制 | ① 生产链路**还没有读状态的人**：S1 只提供存取与校验，谁生成状态（主进程还是扩展提案）由后续切片定；② 因此"损坏丢弃"在真实运行中暂时只由删除清理触发，读路径只有单测覆盖；③ `contextstate` 只覆盖删除清理这一条真实链路；④ §13.5 的三个待拍板点仍未定（S1 不涉及默认数值与 UI） |

---

## 15. 阶段 4 · S2–S6 落地记录：扩展执行层 + Recall + 降级（2026-09-17）

> S1（State / Archive 基础设施）见 §14。本节记 S2–S6 这一批**真的动了发给模型的消息**
> 的部分，以及一件必须先说清楚的事：**本批不新增第二次模型调用**（用户明确把
> Deep Context 的执行推迟到 N21-8）。因此「需要语义理解的生成」与「确定性的变换」
> 被分开交付，下面的表按这个分界逐条标了状态。

### 15.1 交付了什么

| 切片 | 内容 | 状态 |
|---|---|---|
| **S2 Tool Sweep** | `resources/pi-extensions/context-transform.js` 的 `planToolSweep` / `applyToolSweep` + `context.js` 的 `context` 钩子：把 `recentTail` 之外的大块 `toolResult` 换成墓碑（`Tool/Target/Original tokens/Ref`），原文不动，归档元数据写 `context-state/<id>.archive.json` | ✅ 完成并真实验证 |
| **S3 Episode Fold（装配层）** | `mergeEpisodeRefs`（只加引用、不重新摘要）、`episodeRecursionRisk`（§12.7 预检）、S1 的 `EpisodeState` schema 校验；**语义生成器**（把一段历史变成 decisions/nextActions）没有实现 | ⚠️ 装配层完成；生成器被**切片边界**推迟（见 15.3；能力上扩展内即可完成，§16.1） |
| **S4 Structured Compaction（闸门 + 降级）** | `buildStructuredSummary`（`<TASK_STATE>` + `<HISTORICAL_CONTEXT>` 两段式、六类字段齐备才接管）+ `session_before_compact` 钩子：只换摘要文本、保留 pi 的 `firstKeptEntryId` 与 `tokensBefore`；缺字段/无状态/水位不符一律 `return undefined` 交回 pi 摘要 | ✅ 闸门与降级路径完成并单测；真实长会话的「六类字段逐一非空」取证要等生成器 |
| **S5 Recall + 预算 / TTL / 审计** | `context_recall` 工具（原文从会话条目读回，不存副本）、`recallBudget`（单次 / 累计 / 条数，超限**拒绝并解释**、不截断）、`wrapRecall`/`parseRecall`/`stripStaleRecalls`（回合号 TTL，正文清成只留引用的存根）、`<id>.recall.jsonl` 审计（时间 / 大小 / 发起方 / ref） | ✅ 完成：单测直调工具 execute 覆盖全部拒绝分支；真实回合里模型确实调用并取回原文（见 15.4） |
| **S6 fallback + diagnostics** | 所有钩子 `try/catch`，任何异常返回 `undefined`（整轮不改消息）；`sweepViolations`（复用 N21-10 的 `violations`）非空即放弃；诊断 `YAN_CONTEXT_EXT_LOG` 每次变换一行 JSON | ✅ 完成：live 断言「有 swept≥1 且 0 条 error」 |

**默认接管集（2026-09-17 拍板）：`['tool-sweep', 'recall', 'compaction']`。**
扩展只在 `ContextPolicy.kinds` 里出现对应项时才动上下文；`kinds` 的唯一真源仍是主进程的
`ContextPolicy`，扩展从 `YAN_CONTEXT_POLICY` 环境变量读到同一份（pi 子进程继承 `process.env`）。
用户的原话是「Tool Sweep 应该默认打开，但同时要保留必要的引用」，落到代码是两件事：

1. **清理默认开**（墓碑化而非删除：原文只在发给模型的窗口里换成
   `Tool / Target / Original tokens / Ref` + 取回说明，会话文件一行不动）；
2. **召回必须同时在线**（`recall` 与 `tool-sweep` 捆绑默认开）—— 墓碑引用的唯一取回通道就是
   `context_recall`，少了它，默认开启的清扫会变成“拿掉且取不回”；
3. **正在使用的引用不得清扫**（硬约束①）：`activePathsOf()` 从**当前用户回合**的
   编辑/读取目标收路径，`sweepCandidates` 跳过它们；旧回合的编辑不在此列（否则硬留集
   单调增长，清扫永远压不动）。

`episode-fold` 仍不在默认里（它要状态生成器，见 15.3）；想回到旧行为把 `kinds` 覆盖成
`['compaction']` 即可。

### 15.2 关键实现决定（别改回去）

| 决定 | 为什么 |
|---|---|
| 扩展**不写**状态文件，只写归档**元数据** + 召回账本/审计 | 状态 schema 的真源是 `src/shared/context-state.ts`（TS），扩展侧复制一份校验必然漂移，所以写入必须走能拿到这套校验的路径。**注意（§16.1 更正）**：这不等于"扩展不能生成状态" —— 生成用的模型调用在扩展内就能做，待解决的是"生成物怎么过 TS 校验再落盘"（§16.3） |
| 原文从**会话条目**读回（`sessionManager.getEntry`），不存副本 | 「原始 JSONL 是事实源」；S1 的 `ArchiveEntry.contentStored` 保持 `false`，归档只是索引 |
| 消息 ↔ 原始 entry id 对齐用 **数量 + 角色双校验**，任一不满足就整轮放弃 | 错位的 entry id 会让 `ctx://` 引用指向别人的历史（比不做 sweep 危险得多）。复刻了 pi 的 `buildContextEntries`（压缩后只从 `firstKeptEntryId` 起），否则压缩过的会话 id 会全部错位 |
| `toolResult` 只换 `content`，保留 `toolCallId` / 顺序；**从不删除消息** | tool_call 与 tool_result 的原子配对是 §12.4 硬约束；`context-safety.violations()` 在提交前复核 |
| Task State 用 `role: 'custom'`（`customType: 'yan-task-state'`）注入在历史之前 | pi 的 `convertToLlm` 把 custom → user；用 custom 是为了可识别（幂等）且**不参与用户回合计数**（TTL 依赖回合号） |
| TTL 用「召回包装里的回合号 < 当前用户回合数」判定 | 召回内容在**本回合**必须留着（工具循环还会再请求），下一轮用户输入到来才是清理点。纯函数，可单测 |
| 会话删除要一并清 `<id>.recall.json` / `.recall.jsonl` | 它们与状态/归档同属按 sessionId 命名的派生物，漏了会在用户目录里留下孤儿 |

### 15.3 明确的边界：语义生成被**推迟**，而不是扩展做不到

> **2026-09-17 复核更正（见 §16.1）**：本节初版写的是「pi 扩展侧**没有**『跑一次推理』的 API」——
> 那是**误判**。扩展上下文里有 `ctx.modelRegistry` / `ctx.model`，而
> `ModelRegistry.complete(model, context, options)` 就是一次无工具的模型调用。
> 结论：生成器**不需要**第二个 pi 进程，也不需要为「多起一个进程」拍板；
> 唯一还需要用户拍板的是**额度与延迟**。

S2–S6 没有生成器，是**切片边界**（用户明确把第二次模型调用 / Deep Context 推迟到 N21-8），
不是能力所限：

- `EpisodeState` 的 `decisions` / `outcome` / `failedAttempts` 等**语义内容**没有生成器；
- `TaskState`（`CodingState`）的增量 delta、`nextActions`、`assumptions` 同理；
- 于是 `session_before_compact` 现在**实际上总是走降级路径**（没有状态文件就交回 pi 摘要）——
  接管逻辑本身有单测与「种一份状态」的验证，等生成器落地即自动生效。

生成器的落地形态（N21-4 剩余项 / N21-5）已经明确：在 `agent_settled` 边界上由扩展自己调
`ctx.modelRegistry.complete(ctx.model, { messages }, { tools: [] })` 跑一次归纳
（严格 JSON 输出；水位、`generation` 这类**元数据由宿主附加**，不接受模型自报），
再走主进程/store 的校验与原子写。**不伪造**结构化状态：机械拼出来的「摘要」缺 decisions，
比不上 pi 自己的摘要。

### 15.4 证据（2026-09-17）

| 六栏 | 内容 |
|---|---|
| 实现 | `resources/pi-extensions/context-transform.js`（纯逻辑：适配 / sweep / 注入 / recall 预算与 TTL / 结构化装配 / 诊断）、`resources/pi-extensions/context.js`（钩子与工具 + 归档落盘）、`src/main/index.ts` + `src/main/agent.ts`（注册扩展）、`src/main/context-state-store.ts`（删会话时一并清 recall 账本/审计） |
| 自动检查 | `npm run typecheck` 干净；`npm run test:unit` **1148/1148**（`test-context-transform.mjs` 覆盖：entry 对齐 / sweep 规划与幂等 / 墓碑 / Task State 注入 / recall 预算与 TTL / 召回账本回合号 / 结构化装配与降级 / **JS 产出的归档文件过 S1 的 TS schema** / 直调 `context_recall.execute` 覆盖成功与全部拒绝分支 / 诊断日志；本轮新增 toolPaths / activePathsOf 与「默认接管集 + 硬约束①」两组断言。`test-context-policy.mjs` 断言默认 kinds 与“下一步”阶段）；S1 的 `test-context-state.mjs` 补 2 条（文件级递归摘要判定）并修掉一个真实缺口（整份状态校验时没传 `knownEpisodeIds`，只有单测显式传参才拦得住 Episode→Episode 引用） |
| 真实运行 | `test:live -- contextsweep`（cost 1，已进 `npm run check`）：真实 Electron + 真实 pi 里跑三个回合，第一个回合产生 8893 字符的 bash 输出（`bash 8893`），第二个回合后退出检查确认 **归档元数据已写出、`ctx://tool/<entryId>` 指得回原始条目、诊断里 `swept≥1`、0 条 error**，而且模型**真的调用 `context_recall` 取回了原文**（审计 `result=ok, tokens=2224`，包装回合号 = 2）；第三回合后确认 **上一轮的召回正文已被清成存根**（诊断 `expiredRecalls≥1`）——§12.11 第 9 条的真实证据。同一批次回归 `contextstate`（删会话清派生状态）与 `trash`（真删 + 撤销）通过。随后用用户指定的 `YAN_TEST_MODEL=commandcode/longcat-2.0:free` 独立重跑并通过同一组断言；不可用或触顶时备用 `commandcode/laguna-s-2.1-free`。**这一轮还抳出一个真实缺陷并修掉**：召回账本原本只在“已有召回”时才写，导致首个召回拿到的回合号是 0、会在同一回合的下一个请求里被误判过期（实测表现为模型被迫二次召回）—— 现在只要 `tool-sweep`/`recall` 已接管就每轮记当轮回合号。**2026-09-17「清理默认开」轮次**：`test:live -- contextsweep` 改为**不设 `kinds`** 重跑通过（验的是默认接管集本身：bash 8893、归档 1 条、`ctx://` 指得回原始条目、`swept≥1`、recall `tokens=2224`、`expiredRecalls≥1`、0 error） |
| 视觉验收 | 墓碑本身不改界面（只影响发给模型的消息，会话转录与工具卡片仍显示原文）。但**「清理默认开」改了右栏的阶段接管状态**，所以重跑了受影响的 3 张：`matrix-contextbudget-{dark,light}-2026-09-17.png`、`matrix-ctxnarrow-1440x900-100-dark-2026-09-17.png`（`YAN_MATRIX_ONLY=contextbudget,ctxnarrow`，组 0/1 全绿、溢出 0px）—— 新图里「下一步」显示**清理旧工具输出（约 168k 时）**、清理阶段标记已接管；旧批次 `-2026-09-16` 保留作对照 |
| 应用与包 | **未验证** —— 与其它 N21 项一样，共同缺口是 P0-8（用户要求放到最后）；`out/` 构建成功、`vendor:pi:check` 通过 |
| 剩余限制 | ① EpisodeState / TaskState 生成器未实现（**切片边界**：第二次模型调用被推迟到 N21-8；能力上扩展内即可完成，见 15.3 与 §16.1），因此结构化压缩实际走降级；② 三阶段的**独立 Rearm / Cooldown**（§12.3）未接（sweep 是幂等的，重复尝试无副作用；fold/compaction 需要时随 N21-5 的状态层一起做）；③ `kinds` 默认已改为 `['tool-sweep', 'recall', 'compaction']`（用户 2026-09-17 拍板：清理默认开、
但保留可召回引用；见 15.5 与 15.1）—— 想回到「原文全留」就把它覆盖成 `['compaction']`；④ §12.11 第 10 条「连续 20+ 长回合压力测试」未做；⑤ `recentTail` / 门槛数值仍是保守初值，未做压力测试标定（§12.12 的 P2） |

### 15.5 需要用户拍板（新增一条，接 §13.5）

4. ~~**Tool Sweep 是否默认开启**~~ → **已拍板（2026-09-17）：默认开启，但保留必要引用**。
   用户原话：「Tool Sweep 应该默认打开，但同时要保留必要的引用」。落地：
   - `DEFAULT_CONTEXT_POLICY.kinds = ['tool-sweep', 'recall', 'compaction']`（扩展侧 `context.js` 的
     `policy()` 同步，两边必须一致，否则界面与真实行为不同）；
   - `recall` 与 `tool-sweep` 捆绑：这是「引用」两个字的机制落点；
   - 硬约束①实装：清扫跳过**本回合正在动**的文件（`toolPaths` + `activePathsOf` + `sweepCandidates`）；
   - 未解决错误、用户约束、assistant 正文的保护规则不变；
   - 设置面板开关仍属 **N21-7**（阈值/开关可配置化），本次只改默认值。
   - 验证：`test-context-transform.mjs` 的 K 段（默认 kinds 真会清扫 + 关掉即回到不改消息 +
     activePaths 保护生效）、`test-context-policy.mjs` 的默认接管集断言、
     `probe/context-budget.js` 的标记接管状态断言。

---

## 16. 能力复核更正与外部评审要点（2026-09-17）

### 16.1 复核更正：扩展侧**能**自己做一次无工具模型调用

§13.3 初版把「扩展能不能自己调模型」判为「❌ 没有」，因此 §15.3 与 N21-8 都把生成器
安排在「砚侧起独立 pi 进程」上。**这个前提是错的**，复核后更正如下。

| 事实 | 源码依据（`resources/pi-runtime/dist/bundle/chunks/chunk-JVUZSMYM.js`，pi 0.85.1） |
|---|---|
| 扩展钩子的 `ctx` 带模型注册表与当前模型 | `ExtensionRunner.createContext()`：`get modelRegistry(){…}`、`get model(){…}`（`getScopedModels` / `get thinkingLevel` 也在） |
| 一次无工具调用就是一个方法 | `ModelRegistry=class{constructor(runtime){this.runtime=runtime} … complete(model,context,options){return this.runtime.complete(model,context,options)}}`（返回 Promise） |
| 「这一轮真的结束」对扩展可见 | `async _emitAgentSettled(){…; await this._extensionRunner.emit({type:'agent_settled'}) …}`；它在 `while(…) await this.agent.continue()` 循环**结束后**才 emit（retry / queued follow-up 都跑完） |
| 扩展能读到当前上下文用量 | `ctx.getContextUsage()`（`getContextUsage:()=>(runner.assertActive(),runner.getContextUsageFn())`）—— 判定用的用量不必由桌面端再算一份 |
| RPC 能看到扩展注册的 command | RPC 的 `get_commands` 返回 `session.extensionRunner.getRegisteredCommands()`，条目带 `source:"extension"`（与 `registerShortcut` 在 RPC 下无法枚举/执行是**两件事**） |

**对排期的影响**：

- N21-4 剩余的语义生成器、N21-5 的状态增量更新、N21-8 的 Pass 1，都可以**在扩展内**完成 ——
  不需要第二个 pi 进程，也不需要 sidecar RPC。
- §13.5 第 1 条（Deep Context 是否在范围内）要拍板的只剩**额度与延迟**。
- `main/title.ts` 的独立进程模式降为**备选**（需要多轮归纳、或要用另一个模型/额度跑归纳时才用）。
- 本批（S2–S6）没做生成器仍是事实，但原因**只是切片边界**，不是能力限制。

**可复用经验**：在 minified bundle 里按**字段名**推断能力会误判 —— `ctx.model` 看着只是
「当前模型信息」，真正能调的是 `ctx.modelRegistry.complete()`。核实一条能力要同时找
「字段/对象存在」与「方法存在」，不要停在第一层。

### 16.2 外部评审要点（来源：ChatGPT 架构评审）

> 状态：**待评估，未纳入任何切片**。这里只登记要点与我们的初步判断，避免下次开工重新讨论一遍。
> 其中「扩展可自调模型」那条与 §16.1 同源，已更正进本方案。
> **回答原文存档**：[参考 · 上下文管理外部评审（ChatGPT 原文）](参考-上下文管理外部评审（ChatGPT·原文）-2026-09-17.md)（文字未改，仅供核对）。

| # | 评审意见 | 我们的初步判断 |
|---|---|---|
| 1 | TaskState 应定位为**可重建的 materialized view**，不是事实源；原始转录/归档才是证据 | ✅ 与 §13.4 同向（`sourceRange` + 归档只存元数据），值得把这句话写进 schema 注释 |
| 2 | 「状态存在 + 水位完全一致 + 六类字段齐备，否则不用」= 把数据质量当成可用性总闸门；应改**字段级失效**（`objective`/用户约束有效，`hypothesis`/`nextActions` 可 stale 照用） | ⭕ 有道理；与 §12.6 的 durability 分层相容。改动面在 `context-transform.js` 的注入判定与 `inspectContextStateFile`，必须配单测 |
| 3 | 「完全水位一致」在长会话会形成 availability cliff（快照完成时 head 已前进 → 永远丢弃）；应改 **ancestry 正确 + 有界陈旧 + delta 分类叠加** | ⭕ 最值得做的一条：当前精确匹配确实会让异步生成几乎永不生效 |
| 4 | 生成器用 Hybrid：确定性 reducer（files/commands/tests/symbols，零模型成本）+ `agent_settled` 时的语义归纳；**不要每个 tool call 后调模型**；输入 delta、输出**完整快照**而非 patch | ✅ 与 §12.2/§12.6 相容，可直接作为 N21-5 的默认形态 |
| 5 | `<TASK_STATE>` 顺序改 `SYSTEM → HISTORICAL → TASK_STATE → RECENT_RAW → QUERY`；system 里只放「权威级别协议」，状态作为 **data** 注入（防止工具输出/网页文本经状态间接获得 system 权限） | ⭕ 位置这条要**实测**（`language` 扩展已经证明过「位置决定服从率」）；system 只放协议的思路与 §12.7 同向，倾向采纳 |
| 6 | Tool Sweep 可保守默认开，但要 allowlist（不扫 active diff / 未解决失败的测试 / 当前 stack trace）+ **富墓碑**（tool/command/status/size/ref/checksum 与首末错误行）；顺序必须 `归档落盘 → 校验 checksum → 再墓碑`；同一 ref 短时间重复 recall 应 **pin** 而不是死守 TTL=1 | ⭕ 富墓碑与顺序建议采纳（现有墓碑只有 Tool/Target/Tokens/Ref）；默认开仍属 §15.5 的产品决定 |
| 7 | 补状态事务协议：`generation` / `basedOnEntryId` / `branchId` / `dirtyDomains` + temp → fsync → atomic rename | ⭕ envelope 已有 `schemaVersion`/`sessionId`/`sourceWatermark` 与原子写；缺 `generation`/CAS 与 fsync，随 N21-5 补 |
| 8 | 策略公式要更强的下界：`workingSet ≥ systemFloor + taskStateMax + recentRawMin + queryReserve`（否则只是顺序安全，不保证上下文结构能生存） | ⭕ `workingSet <= 0` 已返回 `null` 降级（`shared/context-policy.ts`），但确实还没有「装得下结构化上下文」这条约束 |
| 9 | 如果只能做一件事：先补 **State Producer + Freshness Protocol** | ✅ 与我们的收敛一致 —— 这是唯一阻塞闭环的事 |

### 16.4 第二轮：额外模型调用的可控方案（外部建议，**尚未采纳**）

用户的决定：接受"必要时多一次无工具 completion"，但要求 **智能调用 + 手动调用 + 开关** 三层可控
（额度走订阅制 OAuth，额外调用会吃订阅配额）。第二轮把这个要求细化成下面的方案
（原文见[存档的第二轮问答](参考-上下文管理外部评审（ChatGPT·原文）-2026-09-17.md)）。
**状态：待用户拍板，未纳入任何切片。**

| 议题 | 建议（原文口径，不是我们的结论） |
|---|---|
| 触发信号 | 不做 boolean，四类：项目语义源 hash 变化 / 单条语义输入 ≥ `max(8k, 5%W)` / 累积语义增量 ≥ `max(12k, 8%W)` / 水位到 65%・82%（且 semanticDirty） |
| "项目有备注" | AGENTS.md 类文件 **变化才触发**（内容 hash，不看 mtime）；若新增项目备注字段，同样按 contentHash 触发，且备注本身作为权威 context 注入、TaskState 只做派生视图（避免归纳失真弄丢用户约束） |
| "用户发了很多上下文" | 按**语义权重**算，不按条数/字符数：日志・stack trace ×0.1、代码块 ×0.25、tool result ×0；20k 日志不应触发刷新（属 `evidenceDirty`，走 archive + 确定性 reducer） |
| 判定 owner | 扩展：`agent_settled` 时一次 `shouldRefreshTaskState()`；Electron 只下发 policy（带 version）与开关，不再自己算 token 再 RPC 下令（避免 split-brain） |
| 手动调用 | 输入框附近的「上下文 ▾」菜单（设置面板管默认值，不管日常操作）；`force=true` 无视 dirty/阈值/冷却，但**不**无视 in-flight、会话运行中、ancestry 与会话身份 |
| 开关 | 三层继承（全局 → 项目 → 会话 `inherit/on/off`）；`autoSemanticRefresh` 默认 **off / unconfigured**（首次开启时告知会消耗订阅额度）；`Tool Sweep` 与「自动上下文管理」默认 **on** |
| 可观测 | 不显示美元估价（OAuth 下 token ≠ quota 单位）：调用次数 + provider usage（Input/Output/Cache Read）+ 触发原因枚举（`manual` / `project_context_changed` / …）；不 toast |
| 小窗口 | 维持 `null`（**不** clamp 成"最小正工作集"），但升级为结构化 disabled 结果（reason + `fallback: pi-native`），并加质量门 `minManagedWorkingSet = 32k`：低于它也不介入 |
| 优先三条 | ① freshness / ancestry / delta overlay；② Hybrid State Producer（把 #7 的注入顺序与权限分离当作它的 DoD）；③ 安全 Tool Sweep + archive/recall |
| Deep Context | 现在不做（维持 N21-8 的定位）；等普通 TaskState 闭环 + A/B（指标清单见存档）后再决定 |
| model profile | 保留当前默认值，但先把 `workingSetCap` / `workingSetRatio` / `responseReserve` 三个字段 profile 化（lookup：specific → provider → generic） |
| 状态文件是用户数据 | 算：设置里要能查看 / 打开目录 / 清除（会话·项目·全部），且"清除 TaskState"不得连带删 archive 与原始转录 |

**需要用户拍板的四点**：① `autoSemanticRefresh` 默认值（off 还是 unconfigured 引导）；
② 是否新增"项目备注"字段（还是只认 AGENTS.md 这类文件的内容 hash）；
③ `minManagedWorkingSet = 32k` 这道质量门要不要；④ 手动刷新入口的位置。

### 16.5 第三方核对（Codex）：与 ChatGPT 的一致处与分歧

同一个方案又交给**本机 Codex CLI**（`gpt-5.6-luna`、reasoning max、只读沙箱、`--ephemeral`，
**147,856 tokens**）核对了一遍；原文见
[参考 · 上下文管理外部评审（Codex 原文）](参考-上下文管理外部评审（Codex·原文）-2026-09-17.md)。
**状态：与 §16.2 / §16.4 同级，待评估、未排期。**

**两边一致的部分**（可直接当共识）：

- 状态是可重建的 materialized view（JSONL 才是事实源），判据是任何快照都能由 `sourceRange + watermark` 重建。
- 精确水位匹配会形成 availability cliff → 改 ancestry + 有界陈旧 + delta overlay（Codex：首版可允许**陈旧最多 2 个 settled turn**，换分支或越界立即丢弃）。
- 生成器 = 确定性 reducer + `agent_settled` 时一次无工具 completion；输出**完整快照**而非 patch；
  Codex 额外要求**单飞（不并发）、超时降级**。
- 扩展内 `agent_settled + ctx.modelRegistry.complete()` 可行，不需要 sidecar。
- system 只放"权威级别协议"、状态作为 data 注入；但**必须用真实序列实测**，
  验收要看到 `SYSTEM→HISTORICAL→TASK_STATE→RECENT_RAW→QUERY` 且状态文本拿不到 system 权限。
- 只做一件事：先补 **State Producer + Freshness Protocol**（并用真实模型证明 `<TASK_STATE>` 真的注入了、
  压缩接管不再永远降级）。

**分歧点**（按差异大小排）：

| 议题 | ChatGPT | Codex | 差异性质 |
|---|---|---|---|
| stale 字段能不能照用 | 可照用（`hypothesis`/`nextActions` 标 stale 仍注入） | **不完全同意**：必须标记或剔除，不能静默当有效；空数组要能表示"已确认为空" | 安全口径之争（影响 `renderTaskState` 实现） |
| 质量门 | 给常数 `minManagedWorkingSet = 32k` | 要质量门但**不写死常数**：按 `systemFloor + taskStateMax + recentRawMin + queryReserve` 判 | 常数 vs 结构性判据 |
| 触发数字与语义权重 | 8k / 12k / 65% / 82% + 日志×0.1、代码×0.25、tool×0 | 作**可调启发式**，不写死；`tool result = 0` 只适用于**语义刷新**，不得用于证据归档与 reducer | 可调性与适用范围 |
| Tool Sweep 默认开 | 建议默认开 | **有前提**：无条件默认开不完全同意（要先有失败注入/压力测试） | 上线时机 |
| 项目备注字段 | 建议做成新字段（作为权威 context） | 归入"可后置" | 排期 |

**Codex 补的、两边都没提的四项**（值得当硬性工程要求）：

1. TaskState **自身的**最大 token / 字段优先级 / 超限裁剪规则（状态本身也会膨胀）。
2. 生成器的**重入、取消、迟到结果 CAS** —— 不能用旧结果覆盖更新的快照。
3. **归档与墓碑提交失败时的原子回退**（与 §16.2 #6 的"先 archive 后墓碑"配套）。
4. 真实模型对"状态顺序 / 约束优先级 / 分支切换"的验收（不能只靠单测）。

**可后置清单（Codex 口径）**：Deep Context、项目备注字段、三层继承设置、详细成本面板、
model profile、复杂 pin、精确触发数字 —— 都不阻塞第一个闭环。

### 16.3 更正之后新出现的待决项（生成器开工前要定）

§16.1 把生成器从"侧进程"挪回扩展内之后，有三件事必须先有结论：

| # | 待决项 | 选项与倾向 |
|---|---|---|
| 1 | **生成物怎么过 TS schema 校验再落盘**（扩展是 JS 子进程，schema 真源在 `src/shared/context-state.ts`） | (a) 扩展调用一次宿主侧的校验/落盘通道，由主进程 `saveContextState` 先校验后原子写（**倾向**：与 §15.2「写入走能拿到校验的路径」一致，但要新增一条扩展→主进程的调用路径）；(b) 扩展直接写、主进程**读**时校验并丢弃（读路径已就绪，但失败面朝用户）；(c) 扩展侧再实现一份校验 + 与 TS 的交叉单测（重复真源，不做） |
| 2 | **这次调用花的是谁的额度** | 用当前会话模型（`ctx.model`）最省事，但会把归纳成本算进同一份订阅/配额；若要固定用便宜模型，就需要 `ctx.scopedModels` 里挑一个并写进状态（`generatorModel` 字段已在 §16.2 #7 提到）。**要与用户确认** |
| 3 | **放在 `agent_settled` 里同步等会不会拖慢用户** | 归纳是一次 completion（秒级到十几秒），若阻塞在"回合结束"与"下一次输入"之间就会被感知。需要实测延迟，或改成"落盘后下一轮才生效"的异步形态（与 §16.2 #3 的 freshness 方案配套） |

除此之外，§13.5 的四个拍板点（Deep Context 范围、默认数值、状态文件可见性、Tool Sweep 默认开关）仍然有效；
第二轮对这三项给的具体建议见 §16.4。

## 16.6 第三轮外部评审：实施级方案与我们的核查（2026-09-17 晚）

> 来源与状态：ChatGPT 网页版，由砚的**内置浏览器直接驱动**（会话 `chatgpt.com/c/6aab1183-…`，思考 2m 18s），
> 提问是「针对 N21-4 剩余 5 项给实施级方案」。**原文存档**：
> [参考 · 上下文管理外部评审（ChatGPT 原文）§六/§七](参考-上下文管理外部评审（ChatGPT·原文）-2026-09-17.md)。
> 状态：**要点已核实（见下"核查更正"），但整体仍是待采纳的外部意见**，不构成决定；
> 采纳哪些、按什么顺序做，以本节的"对 N21-5 的收窄"和 §12.12 为准。

### 16.6.1 与既有结论一致的部分（三方可当共识）

- 状态是**可重建的 materialized view**：`TaskState = SemanticState + EvidenceState + FreshnessEnvelope`，
  模型只产出 `objective/constraints/assumptions/hypothesis/nextActions`；`files/commands/tests/symbols`
  由**确定性 reducer** 产出，落盘前**覆盖**模型返回的同名字段（防幻觉污染 durable state）。
- **Abort 不是正确性机制**，CAS 才是：超时/取消只省算力，"迟到结果"必须靠
  `expectedRevision + epoch + generationId + ancestry` 在主进程侧 reject。这与 §16.5 Codex 的
  "生成器重入/迟到结果 CAS"是**两个独立来源的一致要求**。
- 精确水位匹配是 availability cliff → 改 **ancestry 正确 + 有界陈旧 + delta overlay**（同 §16.5）。
- 单飞（single-flight）+ 合并 pending；超时（建议 20s）**保留旧状态**，绝不落半成品、不阻塞 agent。
- `system` 只放协议，状态作为 **data** 注入；顺序需用真实序列实测，不能只靠单测。

### 16.6.2 它给的、可直接当 N21-5 契约的字段级内容

| 议题 | 它的具体口径 |
|---|---|
| coordinator 状态机 | `epoch`（session switch / fork / 分支切换 / extension reload / state reset 时 `epoch++` 并 abort+pending 清空；**普通 compaction 不 bump**）+ `committed{revision,sourceHeadEntryId,sourceSettledSeq,hash}` + `inFlight{generationId,epoch,baseRevision,targetHeadEntryId,targetSettledSeq,deltaHash,dirtyMask,AbortController}` + `pending` |
| CAS precondition | `saveContextState({candidate, precondition:{lineageId,epoch,generationId,expectedRevision,targetHeadEntryId,targetSettledSeq}})`；主进程满足全部条件才 `revision=base+1` → temp → fsync → rename |
| freshness 档位 | 读时算 `staleSettledTurns`：gap 0 全用；1–2 用 objective/constraints/assumptions + `hypothesis/nextActions` 标 stale；3–6 丢假设/假设之外三个；>6 丢语义状态。**evidence 四类始终允许 deterministic overlay 补到 live head** |
| overlay | 纯确定性文本块 `<TASK_STATE_DELTA from through settled_gap>`；`recent_user_directives` 只做 verbatim 摘录，**不**启发式改写成新 constraint 再持久化 |
| token 预算 | `TARGET 3500`；hard `min(6000, max(2500, workingSet×0.025))`；overlay ≤2000；裁剪优先级 objective 永不删 → 约束不删 → 旧成功命令留最近 6 / 失败的优先留 → 老 PASS 测试只留"当前失败 + 最近一次关键成功" → symbols 24 / files 40 / assumptions 8 / hypothesis 6 / nextActions 8；超长 evidence 进 archive 只留 `ctx://` |
| 刷新判定 | `Dirty` 位掩码（USER_INTENT/CONSTRAINT/FILE_CHANGE/SYMBOL_CHANGE/COMMAND_RESULT/TEST_CHANGE/FAILURE/ASSUMPTION_RISK/CONTEXT_GROWTH）+ 权重（CONSTRAINT 8、FAILURE 5、TEST_CHANGE 5、USER_INTENT 4、ASSUMPTION_RISK 4、FILE_CHANGE 2、SYMBOL_CHANGE 2、COMMAND_RESULT 1）；`shouldRefresh = hardDirty ‖ score≥6 ‖ settledGap≥2 ‖ deltaTokens≥6000 ‖ stateMissing`；纯只读操作不触发 |
| 注入实现 | 不做"找最后一条 user 再挪到末尾"，而是 `partitionContext()` 切出 `historical / previousRaw / currentTurn`，**currentTurn 整块原样保留**（否则工具 call/result 对会被打散） |
| 验收探针位置 | 断言放 `before_provider_request`（provider payload 构建后仍可整体替换），**不是**直接测 `context` 钩子返回；记录 hash/role/marker/token 数，不落全文 |
| 压缩两段式 | summary 内文本顺序 `HISTORICAL_CONTEXT → TASK_STATE`；生成 HistoricalContext 时**先剥掉 previousSummary 里的旧 `<TASK_STATE>`**，否则多轮压缩会自我漂移；compact **不等待** in-flight 生成（否则把后台增强变成可用性依赖）；`firstKeptEntryId` 必须原样回传 |
| 接管条件 | 从"六类字段齐备"改为 `schema valid ∧ ancestry usable ∧ historical generation valid`；`"tests": []` 是合法状态，不是失败 |

### 16.6.3 核查更正（本轮真查过 `pi 0.85.1` bundle，结论进本文档）

| 它的前提 | 核查结论 |
|---|---|
| 扩展可自调模型 | ✅ 与 §16.1 一致 |
| `agent_settled` 是稳定点 | ✅ `_emitAgentSettled()` 在 `while(...)await agent.continue()` 之后 emit，随后才 `_resolveIdleWaitIfIdle()` |
| 扩展可做 ancestry 判定 | ✅ **可做，但要自己走**：扩展 ctx 暴露 `sessionManager`，其上有 `getEntries()/getLeafId()/getLeafEntry()/getEntry(id)/getChildren(parentId)/buildContextEntries()` —— `walkParent` 有原料 |
| `session_before_compact` payload | ✅ 且**比它假设的更宽**：emit 里带 `branchEntries`（当前分支路径条目），不需要我们自己再拼路径 |
| 别用 stdout 多路复用自定义 RPC | ✅ `runRpcMode` 用 `takeOverStdout()` 独占 stdout |
| "要新造一条扩展↔主进程 IPC"（它列为首选方案 A） | ⚠️ **本项目已有等价且已验证的范式**：内置浏览器扩展走 loopback HTTP bridge（`YAN_BROWSER_BRIDGE_URL` + `YAN_BROWSER_BRIDGE_TOKEN`，服务在 `src/main/browser.ts`）。N21-5 直接**复用这条桥**（可加一条带 token 的 `/context-state` 路由），不新增 named pipe / watcher |

即：§16.3 的第 1 项（生成物怎么过 TS schema 校验再落盘）**有现成答案**——复用浏览器桥 + 主进程
`saveContextState` 校验后原子写，选项 (a) 落地成本比原先估的低。

### 16.6.4 对现有待办的影响

- **N21-5 现在有可执行契约**（16.6.1/16.6.2 的字段与判定），不必再"先设计一遍"；
  仍缺的是**语义生成器的实质内容**，而那是 N21-8/生成器那一项的边界，不是 N21-5 的。
- **§16.3 第 1 项（落盘通道）**：结论倾向它列出的 (a) + 本项目已有的浏览器桥（见 16.6.3），
  不再需要在 (a)/(b)/(c) 之间犹豫。
- **§16.4 的四个拍板点与 §13.5 的其余三点仍然有效**；**§15.5 的 Tool Sweep 默认开关已拍板**
  （2026-09-17：默认开 + 保留必要引用，落点见 §15.5/§15.1）—— 这轮没有替用户做这个产品决定，
  只在同日晚些时候由用户明确给出。
- 它额外建议的两条"上线时机"约束值得记进验收：① 有 `100 次 settle/compact/fast-follow-up race`
  的稳定性证据后再谈默认开启；② `before_provider_request` 级证据才算"注入真的生效"。

### 16.6.5 与 §16.2 / §16.5 的差异汇总（三份外部意见放在一起看）

| 议题 | 本方案现状 | ChatGPT 第一轮 | Codex（§16.5） | 第三轮（本节） |
|---|---|---|---|---|
| stale 字段能否照用 | 只做"水位一致才注入" | 可照用（标 stale） | 必须标记或剔除，不能静默当有效 | 分档：1–2 档标记 stale，3–6 档直接丢 |
| 质量门下界 | `workingSet ≤ 0 → null` | 常数 `minManagedWorkingSet = 32k` | 结构性 `systemFloor+taskStateMax+recentRawMin+queryReserve` | 未直接回答；给了 TaskState 侧的 hard cap 公式 |
| 触发数字 | 只有三档水位 | 8k/12k/65%/82% + 语义权重 | 当可调启发式，不写死 | dirty 位掩码 + 权重 + score≥6，另有 6000 tokens 增量闸 |
| Tool Sweep 默认开 | **已拍板默认开**（2026-09-17，含“保留必要引用”，§15.5） | 建议默认开 | 要有失败注入/压力测试再开 | 未直接回答（已由用户拍板） |
| 额外 completion 的归属 | §16.3 #2 待确认 | 用当前会话模型最省事 | — | 建议 `agent_settled` 单飞 + 20s 超时 + 保留旧状态 |

**共同收敛出的一条**（三份意见都指向它）：先把 **State Producer + Freshness/CAS 协议**做扎实，
再谈 Deep Context / 项目备注 / 成本面板等增强。这与 §16.2 #9、§16.5 "只做一件事"完全同向。

---

## 17. 阶段 4 · S7 落地记录：状态生成器（State Producer + Freshness/CAS，2026-09-17）

> 用户拍板（2026-09-17）：**「可以接受多一次无工具 completion 的额度与延迟」** —— 这是 §13.5 第 1 条
> 与 §16.3 里唯一还缺的产品决定。§16.6.5 那条三方共识（「只能做一件事就先做 State Producer +
> Freshness Protocol」）因此可以开工。

### 17.1 这一步补上了什么

在它之前，`session_before_compact` **实际上总是**降级回 pi 原生摘要 —— 路径、闸门、降级都是好的，
但**没有状态文件**可接管。现在补上了产生状态的那一环：

| 环节 | 实现 |
|---|---|
| 触发点 | 扩展 `agent_settled` 钩子（`resources/pi-extensions/context.js` 的 `onAgentSettled`）。它是「这一轮真的结束」的稳定边界（retry / follow-up 都跑完），也是唯一能拿到完整回合 evidence 的时机 |
| 混合式生成 | ① **确定性 reducer**：`files` / `commandsRun` / `testsRun` 从真实工具调用与真实输出里抄（带真实 `entryId`、退出码、测试计数）；② **一次无工具 completion**：`ctx.modelRegistry.complete(ctx.model, { systemPrompt, messages }, { maxTokens, signal })` 只产出语义字段。落盘前 reducer 的字段**覆盖**模型给的同名字段 —— 模型编不出「跑过什么命令」 |
| 纯逻辑层 | `resources/pi-extensions/context-producer.js`（无 IO、不读 env）：evidence reducer / 提示词 / 解析与清洗 / 合并与 provenance / 裁剪与预算 / freshness / dirty 位掩码 / CAS。**所有判定都在这里**，`context.js` 只负责「取消息 → 调 completion → 落盘」 |
| 防幻觉 | 模型输出的白名单 + 逐字段裁剪；`objective` 缺失即整次生成失败；`files`/`commandsRun`/`testsRun` 只认 reducer；`constraints` 与用户原话逐字对齐（命中 → `kind: 'user'` + 真实 `entryId`，否则老实标 `kind: 'model'` / `hypothesis`，不伪造 provenance） |
| Freshness（有界陈旧） | 读时算 `freshnessOf()`：`same` 全用；`older` 且 gap 1–2 → `hypothesis`/`nextActions`/`assumptions` 标 `[stale]`；gap 3–6 → 只留 objective / constraints / 确定性 evidence；gap > 6 或 `diverged` → 不注入。**完全一致才注入会形成 availability cliff**（异步生成几乎永不生效），这是 §16.2 第 3 条的落地 |
| CAS | 状态文件加 `revision`（TS schema 同步：`ContextStateFile.revision?`）；落盘前 `casAllows()` 要求「磁盘 revision = 生成开始前的 revision」。**Abort 只省算力，不是正确性机制**（§16.6.1）：20 秒的 completion 完成时会话可能已经又跑了几回合 |
| 状态自身预算 | `clipTaskStateToBudget()`：`hard = min(6000, max(2500, workingSet × 0.025))`；裁剪顺序 hypothesis → assumptions → decisions → currentState → completed → symbols → files → commands → tests，**objective / constraints / failedAttempts / unresolved 永不删**（那是「接着干下去」的最小集） |
| 单飞与超时 | 模块级单飞标志（并发时只记一条 `skipped: in-flight`）；`AbortController` + 20s 超时；超时/解析失败/合并失败/CAS 失败**一律保留旧状态文件**，绝不落半成品 |
| 压缩接管条件放宽 | `buildStructuredSummary` 默认 `requiredFields: []`（原来硬要求六类字段齐备）。理由：状态现在是**真实生成**的，「真的没有 failedAttempts」与「忘了问」是两件事；形状合法性已由 schema 校验保证。需要保守判定时仍可显式传 `requiredFields`（单测覆盖这条） |
| 默认行为 | 生成只在 `kinds` 含 **`episode-fold`** 时工作。默认 kinds 是 `['tool-sweep','recall','compaction']`（用户 2026-09-17 拍板），**不含它** —— 所以默认**不调模型、不花钱**，与生成器落地前完全一致 |

### 17.2 关键实现决定（别改回去）

1. **生成与注入解耦**：生成由 `episode-fold` 开关控制，而不是「有 compaction 就生成」。默认 kinds 含
   `compaction`，若按它生成，默认路径会开始花额度 —— 那不是一个切片该悄悄带来的行为变化。
2. **元数据由宿主附加**：`sourceWatermark` / `revision` / `sessionId` 全部由扩展算，**不接受模型自报**
   （模型只产出语义字段）。
3. **不做 pending 重跑**：单飞期间来的 settled 只记一条诊断，等下一个 settled 自然重试。跨事件复用
   旧 `ctx` 不安全（那是 per-event 上下文），为此造一套队列不值得。
4. **诊断行带 `stage: 'producer'`**：`diagnostic()` 会先写 `hook: <第一个参数>` 再展开 payload，
   payload 里的 `hook` 会覆盖它 —— 所以 producer 的行必须自带 `stage` 才能与 context/compact 的行区分开。
5. **扩展直接写 `YAN_DATA_DIR/context-state/<sessionId>.json`**：与 S1–S6 的归档 / 召回账本同一路径。
   §16.3 讨论过的 (a)/(b)/(c) 在实现上早已是 (b)（扩展与主进程共享同一个数据目录），
   主进程读路径（`loadContextState`）继续做 schema 校验与安全丢弃 —— 不需要新造 IPC 通道。

### 17.3 证据（2026-09-17）

| 六栏 | 内容 |
|---|---|
| 实现 | `resources/pi-extensions/context-producer.js`（新，~600 行纯逻辑）、`context.js`（`onAgentSettled` / `produceAndCommit` / `messagePairsFromEntries` / 注入与压缩改 freshness）、`context-transform.js`（`renderTaskState` 标 stale、`buildStructuredSummary` 放宽）、`src/shared/context-state.ts`（`revision` 字段 + 校验） |
| 自动检查 | `npm run typecheck` 干净；`npm run test:unit` **1282/1282**（新增 `scripts/test-context-producer.mjs` ~110 条：evidence reducer / 解析清洗 / 合并与 provenance / 防幻觉覆盖 / 裁剪与预算 / freshness 四档 / dirty 与刷新判定 / CAS / **fake ctx 真落盘 + TS schema 交叉校验** / 三个失败路径不落盘；`test-context-transform.mjs` 的注入与压缩断言随 freshness 与放宽条件同步更新） |
| 真实运行 | `test:live -- contextproduce`（cost 1，**已进 check**）：真实 Electron + 真实 pi + `kinds` 显式含 `episode-fold`。真实回合 1 让模型调一次 bash → 退出后检查：诊断 `stage=producer, hook=committed` **1 次且无 error/rejected/aborted**、状态文件 `revision=1`、`objective` 非空、`commands=1`（来自真实工具调用）、**下一个回合 `injectedTaskState=true`**、状态文件**过主进程 `loadContextState` 校验**（`status=ok`）。同一批次回归：`contextsweep`（cost 1，**默认 kinds 下没有 producer 行** —— 验证“默认不花钱”）、`contextstate` / `contextswitchguard` / `contextbudget`（cost 0）全通过 |
| 视觉验收 | 状态只影响发给模型的消息，不改界面；本轮未新增截图（界面在 `episode-fold` 未默认接管时与此前一致） |
| 应用与包 | 未验证 —— 与其它 N21 项一样，共同缺口是 P0-8 |
| 剩余限制 | ① `episode-fold` **未进默认 kinds**（要不要默认开是产品决定，见 17.4）；② EpisodeState 的语义生成（`outcome` / `decision.reason` 等）未做 —— 目前只生成 TaskState，`episodes` 沿用上一版；③ 增量 delta（Existing State + Delta）未做，每次都产出完整快照（§16.6.2 允许，代价是每次 completion 的输入更多）；④ 三阶段独立 Rearm/Cooldown 仍随 N21-5；⑤ `symbolsTouched` 仍为空（需要语言级解析，不假装）；⑥ 20s 超时与 6000 token 硬上限是保守初值，未做压力标定；⑦ §12.11 第 10 条 20+ 回合压力测试未做 |

### 17.4 需要用户拍板（接 §13.5 / §15.5）

5. **`episode-fold` 是否进默认接管集**：现在它默认关（`kinds` 不含），打开才会生成 +
   注入 `<TASK_STATE>`。打开意味着**每轮结束多一次无工具 completion**（额度已拍板可接受，
   但它是常态成本）。形态上也可以走 §16.4 的「智能调用」闸门（`shouldRefresh` 已经在跑：
   纯只读回合不触发、`stateMissing` / hard dirty / score ≥6 / gap ≥2 / 净增 ≥6000 才生成）——
   即「打开开关 + 保留智能闸门」比「无条件每轮生成」便宜得多。**要不要开、开成什么档，需要拍板。**

