# 构建恢复与 UI 剩余任务交接

日期：2026-09-23。用户最新要求：先查构建失败，再整理剩余待办与交接提示词。两位 GPT-6 Luna 已中断，后续不得假定仍有人继续修改。本文记录交接快照；功能规格仍以活动实施正文为准。

> **已归档（2026-09-24）**：本文是 2026-09-23 构建恢复当轮的交接快照，其中 L1 / L2 / L3 / V-3 等“剩余任务”均已在当日收口；当前状态以 [HANDOFF](../../dev/HANDOFF.md) 顶部和 [实施计划](../README.md) 为准，本文只作追溯。

> 后续：L1 / V-2a 已收口（探针与源码），证据见 [HANDOFF](../../dev/HANDOFF.md) 顶部；本文余下部分是当轮快照，L1 行不再代表当前状态。

## 1. 构建问题已修复

`npm run build` 复现失败于 renderer：`Invalid regular expression … Invalid escape`。根因是未完成的 L1 改动在 `Reasoning.tsx` 的 `fallbackSentences` 闭引号匹配正则中，对双引号写了 `\"`；在 `u` 模式中属于非法转义。已移除多余反斜杠，未改依赖。

- `npm run build`：退出码 0，主进程、preload、renderer 均成功；本次产物时间约 2026-09-23 20:32（Sydney）。日志 [build-recovery](../../../out/build-recovery-20260923.log)。
- `npm run typecheck`：退出码 0，TS、CSS 约定、CSS layer 自检均通过。日志 [typecheck-recovery](../../../out/typecheck-recovery-20260923.log)。
- Zod / Virtua 的 PURE 注释警告仍存在，但不阻塞此次构建，不为此更新依赖。
- `git diff --check` 通过。未重启用户窗口，未打包或提交。

## 2. 构建通过不等于 UI 已交付

隐藏隔离 `npm run test:live -- reasoning` **失败**，退出码 1；没有残留 pi 进程。日志 [reasoning-recovery](../../../out/reasoning-recovery-20260923.log)。

已通过的部分：默认折叠、中英文/连续标点/版本号/emoji 分句、一次点击打开、无推理不留空壳、减少动效首次设置、F6 多段推理位于正文后及节点不重挂。

已定位的测试缺陷：`scripts/probe/reasoning.js` 在第一次点击打开之后，又检查仅折叠时存在的 preview；重复了一组分句断言，随后再次点头部却按“打开”验收，实际已经收起，因此读到 `max-height:none`。后半仍要求旧的 260px 多行裁剪与回合结束自动折叠，和新决定“手动展开状态保留”冲突。应修复用例状态顺序并删除重复段，再验证生产行为；不能据这份混合契约探针直接认定所有展开样式都有故障，也不能删掉失败断言就算通过。

## 3. 剩余任务（按优先级）

| 顺序 | 任务 / 当前真实状态 | 下一步与完成出口 |
|---|---|---|
| 1 | L1 / V-2a 推理：源码半完成，构建已恢复，live 尚失败 | 修 probe 的重复分句/双击顺序/旧契约；保留 F6 顺序和节点身份断言。清理 Reasoning 顶部旧注释、冗余 manual/expanded 双态和旧 JSX 注释；复核 reduced-motion 动态切换、切会话不串状态、用户上滚保持与底部跟随。深浅/长句尾部真实可见性实测，不能只查 textContent。当前 RTL+plaintext 保尾方案待验证。 |
| 2 | L2 / V-2b 导航柄：**未改源码** | 改 ConversationOutline 以会话边缘独立导航槽定位，去掉正文左缘+24px 魔数；正文/命中区不相交；键盘预览、长轨末项、同轮数切会话状态重置。更新 outlinepos 旧“紧贴正文”契约，用足够轮数 fixture，不能把少于3轮跳过算通过。 |
| 3 | L3 / H-8a/b 图标：只有 `icon-state.css` 草稿，**未 import、未生效** | 先审查草稿，不为了消除未引用警告直接接入。补必要状态反馈和 reduced-motion。chat.css 的 --text/--panel/--line/--muted 未定义，artifact/image-progress/subagent-inline 的固定浅字/深底回退尚未修；分别映射 --fg/--bg-2或3/--border/--fg-dim，保留 accent/ok/err 状态语义。实测浅色、禁用/hover/focus/selected及运行态停止。 |
| 4 | V-3 设置异步问题：仅审核入待办 | KnowledgeTab 请求身份/代次隔离、宿主拒绝项目漂移；jumpTo/exportMarkdown 的 IPC reject 就地反馈；先复现，不混入前三片。 |
| 5 | F7/H-6b 后台可靠性：原有未闭环项保留 | 续接持久化不等价于真实启动回执；默认阈值连续交接、崩溃启动边界、跨片段归属按原正文；cost 1 需要模型/额度授权。 |
| 6 | 实施-15 新能力：**方案，未实施** | A-0 盘点已有能力→U-3b/A-1 结构化交付核验→A-2 目标预算；A-3 恢复跟随 F7。不可另建第二个任务调度器。 |

完整主题与提示词见 [审核融合入口](../../plan/active/执行-2026-09-23-审核融合与任务提示词.md)。U-1 已实施不重做；Android 和发布仍冻结。其它历史待办不因本次构建修复消失。

## 4. 当前工作树与接手规则

应用已修改：`src/renderer/src/components/chat/Reasoning.tsx`、`src/renderer/src/styles/chat.css`、`scripts/probe/reasoning.js`；新增未接入 `src/renderer/src/styles/icon-state.css`。另有本轮文档整理修改，两个 D 是旧交接/编排移入 archive/plan，对应归档文件是未跟踪项，不是误删。

AGENTS 已精简，DESIGN 和实施-11/13 已融合新要求；实施-15 与审核提示词已新增；所有活动主题已接入提示词。全仓正式文档链接扫描此前为 0 断链，外部 reference 原始旧链接保留。不要回退这些已授权文档改动。

## 5. 可复制给 GPT-6 Luna 的交接提示词

```text
在 C:\Users\YuDaTou\Desktop\pi-desktop 继续工作，使用 GPT-6 Luna。
先读 AGENTS.md、docs/plan/README.md、docs/dev/HANDOFF.md 顶部，以及 docs/dev/交接-构建恢复与UI剩余任务-2026-09-23.md 和 docs/plan/active/执行-2026-09-23-审核融合与任务提示词.md。
git status --short 保留现有修改。上轮两位 Luna 已停止；工作树是文档整理+未验收的 L1+未接入的 icon-state.css 草稿，不是干净基线。两个删除文档已经移入归档，不要恢复旧入口。
构建错误已经修复：Reasoning.tsx fallbackSentences 的 Unicode 正则双引号多余转义。npm run build/typecheck 已通过；不要重复归因到 Zod/Virtua 注释警告或无依据更新依赖。
先收口 L1。阅读 out/reasoning-recovery-20260923.log，修 reasoning 探针中重复分句段、已打开又点击导致收起的状态顺序及旧260px/自动折叠断言；保留 F6 多段顺序和 DOM 节点身份回归。以产品行为重新组织测试，不能删断言凑绿。完善原位展开、手动阅读保持、最新句尾端可见、动态 reduced-motion 和切会话隔离，清掉过期注释与冗余状态。
L1通过后做 L2 ConversationOutline 靠会话边缘的独立导航槽，保证正文与命中区不重叠，键盘/长会话首末项可达；不是移动标题栏开关。随后做 L3 图标：先审查未import草稿，修 chat.css 未定义主题变量，再完善必要状态动画和减少动效，不引入新库、不重做Logo。
每片独占共享样式，build/live/截图串行执行。源码改后 typecheck→build→定向cost0 live；本轮不调模型。UI另做隔离真实窗口截图并逐张查看，旧图不覆盖，不干扰用户现有窗口。未通过就记剩余限制，不把构建成功当交付。
不要提交、推送、打包或重启用户主窗口，不写真实用户测试数据。新增Agent能力和其它后台主题先保留在待办，不扩大当前三个UI切片；Android/发布继续冻结。
每片在 HANDOFF 回填实现/自动检查/真实运行/视觉验收/应用与包/剩余限制，更新唯一活动正文。交付说明真实通过项、失败项、日志和截图，最后给剩余任务。
```
