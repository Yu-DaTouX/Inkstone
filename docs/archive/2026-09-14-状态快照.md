# 工作区整理与剩余工作（2026-09-14）

> **历史快照，不作当前排期依据。** 当前状态与方案入口统一见 [HANDOFF](../dev/HANDOFF.md)；本文保留当时结论供追溯。


合并后的执行顺序见 [Yan 后续工作方案（合并版）](2026-09-14-工作方案.md)。本文继续作为本次清理和状态盘点记录。

本清单依据本次目录检查、当前源码和 9 月 14 日实施记录整理。旧方案里的未勾选项不自动代表当前未实现；历史测试数字不代表本次重新验收。

## 本次清理

- 删除 120 张 `docs/design/preview/live/frame-*.png` 录屏中间帧，保留最终 `live-working.gif` 和审阅截图。
- 删除两个根目录 `*.tsbuildinfo` 编译缓存，以及 `out/sidebar-review-build.log`、`out/ui-review.log` 两份审阅日志。
- 共删除 124 个文件，34,462,401 字节（32.87 MiB）。没有改应用源码、启动入口或现有构建。
- 保留 `out/` 应用输出、`node_modules/` 依赖、`resources/pi-runtime/` 内置运行时，避免影响当前启动。
- 保留 `release/` 安装包、ZIP、校验清单及解包程序。尤其 `release/砚数据/` 是用户数据，不能随发布目录整体清理。
- 保留 `build/icon.*` 图标、字体比较资料、历史设计、测试脚本及所有已有未提交源码。未跟踪不等于无用。

## 优先收尾

| 优先级 | 工作 | 当前事实与完成条件 |
|---|---|---|
| 高 | 当前版本打包验收 | 9 月 14 日实施记录明确未打包；preload 已改为 CJS。旧 release 文件不能证明新源码可发布。需要重新打包、包内检查和实际启动，并核对升级保留用户数据。 |
| 高 | 子代理写入隔离 | 当前与主会话共用 cwd，未实现独立 worktree；需解决并发写入、结果审阅和合并归属。 |
| 高 | 文件树方案第 16 节 | 当前仍有旧点击交互；完整的单击预览、独立加入上下文、键盘树导航、项目切换请求防串、局部刷新与搜索需要落实和验收。已有附件与文件预览不等于完整文件树完成。 |
| 中 | 子代理自动委派 | 当前是 `/subagent` 与程序化入口；模型主动委派未接通。自有进程适配已存在，上游扩展兼容仍未验证。 |
| 中 | 浏览器逐站授权 | 目前默认拒绝并可查看权限请求，缺少显式站点授权；更底层 DNS 重绑定边界未处理。 |
| 中 | 改动归属 | edit/write 等受支持工具已有前后快照；shell/第三方工具改动不能可靠归属到单次调用。 |
| 中 | 回复档位追溯 | 已有全局三档设置，尚未将本轮采用的档位记录到每轮历史。 |

## 需要真实环境验证或产品决定

- 浏览器 Cookie/同源页面存储复制不等于真实登录迁移成功；需要目标网站验证。外部 Chrome 下载也仍需实际下载验证。
- 左栏搜索清空后的展开/焦点/滚动恢复、置顶分支展示、深层分支折叠和多会话后台状态覆盖仍需收尾或核对。“日常模式”只有禁用入口。
- 极矮窗口、长会话和多工具区组合仍需检查；已有部分原生窗口证据不代表所有组合都通过。
- Command Code 月度上限仍为推算，需真实账户字段核对。
- 密度三档、推理窗口拖拽记忆、受阻任务样式已经实现，不再照旧清单重复列为未开发；密度行高调整、像素图标样张属于后续设计选择。
- 代码签名需要证书；macOS 打包需要相应环境；Linux 包尚未验证；真实登录需要账号后端。扩展快捷键受 pi RPC 上游能力限制。

## 核验范围

本次是文件清理和文档整理，未构建、未运行完整回归、未重启应用。历史记录中的 312 项单测、50 个场景通过只作为当时结果引用，不作为本轮验收结论。工作区有大量已有修改和新文件，提交/发布前需要按最终差异重新审阅。

依据：[实施记录](../archive/2026-09-14-实施记录.md)、[新方案](../archive/2026-09-14-功能升级与UI优化方案.md)、[左右栏评审](../archive/2026-09-14-sidebar-review.md)、当前 `subagents.ts` 与 `FileTree.tsx`。
