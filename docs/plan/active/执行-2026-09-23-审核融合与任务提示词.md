# 审核融合与任务提示词 · 2026-09-23

> 最新状态：L1 / V-2a、L2 / V-2b、L3 / H-8a·b·c、V-0、V-1、V-3（R10·R11 + 四个剩余页）、V-4（结构）、V-6（文档收敛）、A-0…A-3、U-3b、**U-0 / U-4 / U-5 / U-6** 已收口并验收；Windows 外壳图标静态核验已做、包验收待打包。证据见 HANDOFF 顶部与 [证据-V0](../../design/证据-V0-UI基线盘点-2026-09-23.md)、[证据-V1](../../design/证据-V1-控件状态与焦点-2026-09-23.md)；下表是任务归属，不是完成证据。

本页是本轮调度与可复制提示词入口；功能正文仍由原主题持有，不建立第二份实现规范。用户授权先审核、整理文档，然后交 GPT‑6 Luna 实施。新增能力方案可以入待办，但不据此擅自恢复 Android、发布、插件或旧记忆系统。

## 1. 本轮优先顺序

| 次序 | 唯一任务归属 | 交付范围 | 状态 |
|---|---|---|---|
| 1 | 实施-13 V-2a | 推理默认最新一句，点击在当前聊天位置展开 | 已收口（2026-09-23，证据见 HANDOFF 顶部） |
| 2 | 实施-13 V-2b | 会话导航柄移至会话区域边缘，排除遮挡 | 已收口（2026-09-23，证据见 HANDOFF 顶部） |
| 3 | 实施-11 H-8a/b/c，实施-13 V-5 验收 | 现有图标浅色适配与状态动效 | a/b/c 的现有触点已收口；Windows 外壳图标静态核验已做（脚本进 check），装完才看得见的 8 项列成包验收单未验（2026-09-23，见 HANDOFF 顶部），H-8 不整段勾选 |
| 4 | 实施-13 V-0/V-1/V-3 | 按审核问题表补键盘、主题、窄窗与异步状态 | V-0、V-1、V-3（R10·R11）均已收口（2026-09-23，见 HANDOFF 顶部） |
| 5 | 实施-14 F7；实施-11 H-6b | 续接启动回执、失败恢复与跨片段归属 | 保留原待办；模型测试另列 |
| 6 | 实施-12 U-3b；实施-15 A-0…A-3 | 证据可信度、运行预算、恢复、交付核验 | U-3b、A-0、A-1、A-2、A-3 已收口（2026-09-23，见 HANDOFF 顶部） |

U-1 已实施，不重写；H-3/H-9/H-10/U-2…U-6 与 V-1…V-6 保留原依赖。此顺序优先响应本轮三个 UI 需求，不宣称其它开放项已完成。

## 2. 源码审核与问题清单

证据范围：当前源码、现有测试和文档静态审核。用户反馈是复现输入；尚未取得本轮真实窗口截图的视觉问题不写成已视觉确诊。不是全仓安全认证。

| ID / 影响 | 当前证据与触发条件 | 解决方案 / 归属 | 验收出口 |
|---|---|---|---|
| R1 / P1 | `Reasoning.tsx` 中 `open = manual ?? !!streaming`；默认多行，折叠仅在 `!live` 显示 `firstLine`。与本轮最新一句需求冲突 | V-2a：展示层抽取最新句/正在形成的末句；保留原文；单击一次进入有限高全文 | 中文、英文、无标点、emoji、空推理、流式/历史/多段；不改变 TurnView 段落顺序 |
| R2 / P1 | `ConversationOutline.tsx` 通过正文左缘 + 写死 24px 减轨宽定位，并 `Math.max(0, …)`；`redesign.css` 轨宽 44px。窄窗夹到 0 时命中区可能进入正文区 | V-2b：定位依据改为会话可用区域边缘；正文给独立导航槽，取消正文锚定的魔数；不是移动 TitleBar 左栏按钮 | 深浅、左右面板开关、940/900 宽、125/150%；真实 DOM 矩形不与正文交互元素相交 |
| R3 / P2 | `ConversationOutline` 的预览由 mouse over/out 控制；`.outline-track` 最大高 88% 且 `overflow:hidden`，长会话需复核末项可达 | V-2b：focus 与 hover 同等预览；长轨提供受限滚动/可达导航，保留稳定 turn id 与虚拟化跳转 | 键盘能跳到首/中/末轮，窄矮窗最后一项可达；不通过缩小命中区掩盖问题 |
| R4 / P2 | `Icon.tsx` 已统一 sprite；Orb 已 `theme=auto`，依赖 README 声明监听主题。不能断言图标全都不适配。动效分散在 motion/chat/redesign 样式；chat.css 的 artifact/subagent/image 触点使用未定义 --text/--panel 等变量，回落到固定浅字/深底（源码已确认，最终计算样式待实测） | H-8a/b：清点语义、实际颜色来源、状态和例外；currentColor + 主题 token；动作反馈与展开状态动效集中复用 | 实际浅色截图与计算颜色/对比度；运行时切主题；reduced-motion；静态图标不持续旋转 |
| R5 / P2 | Reasoning 的 reduced-motion 只在首次渲染读入 useRef；系统偏好运行中切换不会更新该值 | V-2a 同步用订阅钩子处理偏好变化并清理监听/rAF | 已挂载时切换 reduce 能停动画；返回默认后按策略恢复，无重复 rAF |
| R6 / P1 待证实 | `handoff-runner.ts` 发 resume 后轮询会话文件证据再标 resumed；当前证据无法单独证明目标 run 实际启动 | 实施-14 F7：区分 sent / persisted / started / progressed；启动回执含 operationId、resumeId、runner generation，崩溃恢复不能重复发送 | 注入写入后未启动、启动后未确认、重启三边界；禁止拿 marker 等价为运行完成 |
| R7 / P2 | `workbench.ts` 已有 version=1、tabs、activeTabId 及归一化，AGENTS 仍称没有真正 WorkbenchState；存储版本未显式分支拒绝未知版本 | H-3a：修文档；迁移/未知版本降级策略、资源身份与恢复边界明确。不重造已存在状态层 | 旧/坏/未来版本、重复 tab、失效资源、跨会话隔离用例 |
| R8 / P1 能力缺口 | `shared/goal.ts` 接收非空字符串 evidence 即可允许 completed；证明链接存在/产物有效属于另一层，并非当前完成状态可保证 | U-3b + 实施-15 A-1：保留模型报告与宿主验证两个字段，宿主验证器只读且受路径边界约束 | 伪路径、失效文件、串目标证据不能算“已核验”；旧目标照常读取 |
| R9 / P2 文档 | 旧 HANDOFF-NEXT 复制完整排序、写死 clean checkpoint，且把 Android/发布列当前优先；WORKSPACE 薄层职责仍含已迁走浏览器/提问 | 合并引导、删过期重复队列、修索引与架构归属 | 全文档链接扫描和差异检查；历史证据及外部参考保留 |

| R10 / P1 已收口 | `KnowledgeTab.tsx` refresh 只依赖 enabled，没有请求代次/项目身份 guard；Settings 内容仅以 tab 为 key；action 请求不携带 expectedProjectId，宿主按当时 knowledgeIdentity 选项目 | V-3（2026-09-23 实施）：refresh 依赖 `session.cwd`；`latest-only` 代次丢弃迟到结果；写操作带 `expectedProjectId`，宿主拒绝漂移（带字段才校）。已复现后修复 | 延迟 A 项目请求→切 B→B 先返回→A 后返回；旧列表不可作为 B 的写入目标，已有 revision CAS 保留（live 受 contextBridge 只读限制，代次由单测覆盖） |
| R11 / P2 已收口 | KnowledgeTab 的 jumpTo/exportMarkdown 只有 finally，没有 catch；IPC reject 会成为无就地反馈的 rejected promise | V-3（2026-09-23 实施）：两处补 catch → 就地 notice；失败/取消/拷贝拒绝分支不再静默（live 注入受限，见 HANDOFF） | 模拟 IPC reject/剪贴板拒绝/取消/并发操作，无假成功与 unhandled rejection（reject 一条仅源码证据） |
| R12 / P2 性能待测 | ConversationOutline 每次 messages 变化重做 groupIntoTurns，逐 user 用 all.indexOf；流式更新长历史存在重复全表扫描 | V-0 性能基线→V-2b 后续优化：先测 100/1000 轮与连续 delta，必要时单遍构建索引、缓存稳定摘要；不新建第二份消息真源 | flame/耗时基线与前后采样，首末轮与多段映射不回归；未测不宣称性能问题已解决 |

## 3. 通用任务提示词（复制后加下一节的具体卡片）

```text
在 C:\Users\YuDaTou\Desktop\pi-desktop 工作。先读 AGENTS.md、docs/plan/README.md、docs/dev/HANDOFF.md，以及指定切片正文；git status --short 核对并保留已有修改。使用 GPT-6 Luna。
只实施指定切片及必要依赖修复，不重复已交付工作。不提交、推送、打包，不重启用户正在工作的窗口，不写真实用户测试数据。新增模型测试先核对 TESTING 和模型/额度授权。
先读真实调用链与已有测试；涉及设计先更新 DESIGN 再改样式。主进程能力走宿主/yan 唯一入口；不注册 pi 模型工具。共享文件只有一个 owner，构建/live/截图串行执行。
实现后 typecheck、build，跑与改动对应的有效逻辑测试和隔离 cost 0 live。UI 必须另外看真实窗口截图；截图另存新名，测量不能替代看图。无法完成的验收写剩余限制，不标全完成。
交付列修改文件、复现/前后行为、验证证据，并在 HANDOFF 回填实现/自动检查/真实运行/视觉验收/应用与包/剩余限制六栏；更新切片状态。不要把报告存在当作功能完成。
```

## 4. 本轮可执行卡片提示词

### L1：最新一句推理

```text
执行实施-13 V-2a。以 Reasoning.tsx、TurnView.tsx、推理样式及 reasoning 探针为范围。默认在一行中显示原文最新一句/正在形成的末句，长句尾部可见且不切坏字素；空文本不占位。一次点击在当前聊天位置展开完整原文，max-height min(70vh,620px)，折叠头一直可达。不移到右栏，不开新系统窗口。
展开后用户上滚阅读时保留位置；位于底部才跟随新增内容。保持实施-14 推理/正文分段顺序及历史 JSONL，不更改原文和模型提示词。处理流式结束、切会话、reduced-motion 动态切换。更新旧探针断言以验证真实行为，不能仅删除失败断言。先完成 L1 再交接共享样式。
```

### L2：会话导航柄边缘化

```text
执行实施-13 V-2b。目标是 ConversationOutline（会话导航轨），不是左侧项目栏开关或 Composer 高度手柄。轨道贴会话可用区域左边缘，保留可用命中区并给正文独立避让槽，不用高 z-index 或 pointer-events:none 掩盖遮挡。
复核 44px 轨宽、正文固定 24px 偏移和窄窗 clamp；预览在内容区内夹取并支持键盘焦点，长会话末项可达。保留 turn id、虚拟化跳转及首末轮定位。验证左右栏开关、缩放、宽度设置、深浅主题、正文选字、链接和菜单点击均正常。记录实际截图与矩形相交断言。
```

### L3：图标状态与浅色适配

```text
执行实施-11 H-8a/b，并回填实施-13 V-5 的适用验收。先逐项记录标题栏、导航、推理/工具、输入区、右栏、设置的图标来源和状态。复用 Icon/sprite/currentColor、现有主题与动效 token；不引入另一套库，不统一替换已有品牌图形。
为 hover/focus/press/展开补 120–180ms 的必要反馈；只有真实进行态持续动，停止/完成后停。浅色主题检查默认/hover/disabled/selected/error/focus，功能图形对比度目标 3:1；验证 Orb 的实际 auto 主题，不无依据重写。reduced-motion 下保持状态可辨识。先基础和当前触点，依赖未来 H-3 的触点留 H-8c，不被多工作台依赖阻塞。
```

## 5. 其它待办的任务提示词索引

每行与 §3 通用提示词组合即可执行；开工仍需用户范围授权，冻结项不因有提示词自动启动。

| 正文 | 追加任务提示词 | 当前执行边界 |
|---|---|---|
| 实施-01 | 只复核默认 pi、薄层钩子白名单和宿主唯一入口；S5d 已删的空壳不要重建。列最终发布切换证据差额。 | 发布包冻结 |
| 实施-04 | 先区分已通过 Skill 文件链与其它资源类型；复核来源/hash/取消/并发/授权状态，候选之外保持 fail-closed。 | 不自行获取/安装外部候选 |
| 实施-06 | 只补真实长会话证据限制及与 01 的归属记录；N21-9 不重跑。 | cost 1 单列 |
| 实施-08 | 读取冻结状态，维护链接及真实边界，不执行 Android/配对/TLS/APK。 | 搁置 |
| 实施-09 | 记录当前源码与旧包的证据适用范围，维护发布检查清单。 | 暂无发布计划，不自动全量 check/dist |
| 实施-10 | 核对当前实际产物与图片/PDF链路；使用现有截图看图，补深浅视觉缺口；参考图/PDF限制逐条验证。 | 生图不自动获额度授权 |
| 实施-11 | 按 H-3a→H-9a→H-3b 等依赖推进单片；保留 C-2b/C-6/H-6b 数据工程，不宣称 1M 质量。当前优先 H-8a/b。 | shared/store/RightPanel owner 串行 |
| 实施-12 | U-1 不重做，补菜单正向测试和视觉证据；按正文依赖推进 U-2/U-3/U-0/U-4/U-5/U-6。 | 不重构任务面板 |
| 实施-13 | 当前优先 V-2a/V-2b 与图标适用验收，再按 V-0 问题表推进 V-1/V-3/V-4。 | 每片实图、深浅/窄窗/键盘 |
| 实施-14 | 补 F7 启动确认与恢复边界；先 cost 0 故障注入，再单列默认阈值真实连续交接所需模型额度。 | 不重写 F0–F6 已通过主链 |
| 实施-15 | 先 A-0 盘点已有能力，再设计结构化核验、预算与恢复；复用 U-3b/F7，不发明第二个 scheduler。 | 先方案，后按卡片实施 |

## 6. 调度及文档治理

L1→L2 串行；L3 可由独立 Luna 在隔离文件域并行（仅 Icon/独立图标样式及非推理/导航调用点），禁止写 chat/redesign、DESIGN、i18n、visual-matrix。所有 out 构建、live 与截图由 L1/L2 owner 统一串行集成。主代理负责最后验收与公共文档集成，Luna 每片报告阻碍和六栏证据；不能只发送任务而宣称交付。

文档入口固定：AGENTS（长期边界）→ plan/README（当前队列）→ 本页（调度提示词）→ 各正文（唯一任务规格）→ HANDOFF（证据）。NEXT-SESSION 只留读法；旧日期交接归档；旧并行波次转归档。archive/reference、历史截图和真实数据不清理。全仓文档做链接审计，正文按本轮相关主题深查，不对未读历史报告宣称逐字审核。

## 7. 文档审核收尾记录

全仓引用扫描：`node .audit-deep.cjs`，结果 `out/docs-audit-after-20260923.json`。正式文档 brokenDocLinks 为 0；外部 archive/reference 的一条原始旧链接保留原文；未知 npm 命令 bench:context 已从 TESTING/CODE-MAP/实施-06 的可操作指引改为实际 node 脚本入口，不重新运行 N21-9。历史 HANDOFF/归档的命令作为当时证据保留。

日期交接移至 archive/plan，审计中的 missingTracked 对应有意归档的旧路径（未 stage），不是文件遗失。旧并行编排已归档，并修复正式文档和历史证据指向新位置的相对链接；外部参考原文不改。i18n/IPC 等静态扫描结果不能直接定性故障；分模块注册可能造成扫描假阳性，本轮不据此删除源码。
