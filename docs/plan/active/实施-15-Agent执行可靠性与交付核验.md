# 实施-15 · Agent 执行可靠性与交付核验

日期：2026-09-23。状态：**方案；A-0…A-3 已交付（2026-09-23）**。本轮用户要求思考缺失能力并加入待办。本主题覆盖目标执行的可信闭环，既有自主循环、任务计划、项目知识、子代理、浏览器不是缺失功能。剩余：不可确认副作用的“自动回滚”刻意不做；A-2 的 token 预算依赖 provider 真报 usage（不报就显示未知，不估算）。

任务提示词和当前排序见 [审核融合入口](执行-2026-09-23-审核融合与任务提示词.md)。前置阅读 `shared/goal.ts`、`main/goal-service.ts`、`main/handoff-runner.ts`、实施-12 U-3b 与实施-14 F7。

| 切片 | 用户价值 / 最小设计 | 依赖与文件域 | 验收与任务提示词 |
|---|---|---|---|
| A-0 能力差额盘点 | 把已有 goals/任务/续接/用量/repeat guard 映射到需求，确认未实现部分 | 只读源码与文档；第一片 | 提示词：逐项追踪真实调用链，记录已有/部分/未有及代码位置；若已有等价能力则删除重复任务，不造新框架 |

> **2026-09-23 A-0 收口**：盘点结果见 [能力盘点-A0](../../dev/能力盘点-A0-Agent执行可靠性-2026-09-23.md)（15 项需求 → 已有/部分/未有 + 代码位置）。结论：A-1 只加「宿主只读验证器 + 两个并列字段」、A-2 只加「可选 token/时间预算 + 耗尽可恢复」、A-3 只加「四态回执 + 不可确认副作用的用户选择」；上下文预算、usage 采集、事务框架、任务面板都不新建。U-3b 仍是 A-1 的结构化来源前置。
| A-1 交付证据核验 | 区分“模型报告完成”和“宿主已核验”；证据含 goalId、来源、路径/引用、时间、验证方式与结果；失败原因可点开 | U-3b 提供结构化来源，本片增宿主只读验证与展示；goal/service/shared/ipc | **已交付（2026-09-23）**（只读存在性那一层） |
| A-2 目标级预算与停止理由 | 在既有续行次数/usage基础增加可选 token/时间预算，未知费用显示未知；耗尽先停止安排新轮并可恢复 | A-0、H-6b/H-10b 用量归属；goal/shared、调度入口、目标展示 | **已交付（2026-09-23）**（时间预算与 token 预算均已生效：H-6b 落盘 `outputTokens` 后目标内回合求和；一条都没报才显示未知） |
| A-3 可恢复执行与动作回执 | 恢复视图显示最后确认步骤、在途动作及不确定状态；读写与外部副作用分级，重启不盲目重发 | F7 启动回执先落地；handoff/goal 服务及展示 | **已交付（2026-09-23）**：回执四态 + 恢复视图 + 待核实的人工确认出口（未把 started 当门槛，见 HANDOFF） |

优先顺序 A-0→U-3b/A-1→A-2；A-3 跟随 F7。新后台能力全部走宿主或 `yan` 正式入口，不新增 pi 模型工具、记忆系统或第二个任务面板。活动文档的提案并不自动授权外部写操作或模型额度。

每片先约定旧存储兼容读与迁移失败策略，再做逻辑用例、隔离 cost 0 IPC/live；模型多轮行为和包级行为另列证据。六栏没有齐备前不能标“可靠 Agent 已完成”。
