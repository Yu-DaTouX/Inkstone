# 实施-07 · Git 审查 / 环境菜单 / 来源菜单收尾

> 面向实施 agent；2026-09-18 建立（来源：HANDOFF「四、Git 审查与环境菜单」+
> [方案-Git审查与环境菜单及插件视觉规划](../archive/2026-09-18-Git审查与环境菜单及插件视觉规划.md)，原件已归档）。
> 本文只列**还没做完的**与**改动这一层时必须守住的约束**；已完成部分的六栏证据在归档件 §16.3 / §16.4。

## 0. 一句话结论

G1（只读审查）、G2（写操作）、G3（远程 / PR / 比较）、W1（工作树）、W2（携带改动 + 开新会话）、
S1（来源与附件菜单）、H1（外部链接关联 / 额度入口）、P1 / P2（pi 插件目录与包管理）**主体都已交付**。
剩下的是：W2 的**重绑定**、S1 的两项（网页搜索、定位关联消息）、
P1/P2 的 `pi config`（无编程入口）、G3 的其它托管站，以及几个**明确的已知限制**。

## 1. 已完成（不要重复做）

| 项 | 结论 |
|---|---|
| D0 | 字体 / 颜色规范校正 |
| G1 | 环境菜单 + 只读 Git 审查：范围选择、连续逐文件 diff、变更树、筛选、已查看、图片前后对照、未修改区真展开 |
| G2 | 暂存 / 取消暂存 / 提交 / 切换与新建分支 / 拉取 / 推送，含失败分类与「提交并推送」两步语义 |
| G3 | 推送 / 拉取、远程比较 + 托管网页比较、PR 状态（**直接调 GitHub REST API**，不用 `gh`） |
| W1 | 创建并打开用户工作树：仓库旁边长期目录、三类删除拦截、五条创建拒绝、创建后登记为项目 |
| W2 | 未提交改动的三分迁移（已暂存 / 未暂存 / 未跟踪）、二进制逐字节、目标侧验证、失败整体回滚、源侧一字节不动；**在新工作树开新会话** |
| S1 | `main/sources.ts`：图片按会话隔离写数据目录（指纹命名、同内容幂等）、文件引用只登记路径 + 指纹、移除只删自己存的副本；环境菜单「来源」三类合并 + 筛选 + 「文件不在了」异常态 |
| H1 | 关联外部任务链接（按会话隔离、只接受 http/https、内置浏览器打开、边界文案）；额度已认供应商权威字段 |
| P1 / P2 | 设置「插件」tab：目录入口 + 已装列表 + 安装 / 卸载 / 更新（用户级与项目级），列表带来源 / 许可 / 作用域，详情写明「会执行代码、不做沙箱隔离」；有任务在跑时直接拒绝 |

## 2. 三条硬约束（改动 Git 这一层时**不要拆掉**）

1. **写操作只在一个文件里**：`src/main/git-actions.ts`。
   `git-service.ts` / `git-diff.ts` 里**没有** `add` / `commit` / `switch` / `reset` / `stash` ——
   所以「哪些代码能改用户仓库」是一眼可答的问题。
   **也不要**复用 `subagent-isolation.collectDiff()`（它会 `git add -A`）。
2. **不覆盖用户改动**：不 `stash` / `reset` / `clean` / force push，**不跳过 hook**（不传 `--no-verify`）。
   脏工作区能不能切分支**由 git 自己判**。
3. **预期版本按动作分级**（`commit` 全比 / 未指定起点的新建分支只看 HEAD / 其余不比）。
   分级不是偷懒：一律全比会造出**假冲突**（连点两个文件的暂存、提交后立刻推送都会被拒），
   而假冲突的代价是用户学会无视那句提示。

## 3. 剩余工作

| 项 | 内容 | 规模 | 出口 / 判据 |
|---|---|---|---|
| **W2-重绑定** | 完整「带会话继续」要求：项目权限、相对文件路径、附件授权、上下文派生的**重绑定**；新会话的**来源关系落盘** | 1.5–2 天 | 新会话能正确读到工作树内的相对路径与授权附件；源 / 目标关系可追溯。**做不到就不显示「无缝继续」**（当前已按降级路径写明「不带走历史、权限与附件授权」） |
| **S1-搜索** | 网页来源搜索 | **先决条件未满足** | 方案要求「只在**已发现兼容搜索能力**时启用」。我们没有搜索服务 —— 依赖 [04](实施-04-能力自主选择-MCP与Skill.md) 的 `yan capabilities discover`。**不要**为此新造一个私有搜索后端 |
| **S1-定位消息** | 「定位关联消息」：消息与来源的关联落盘 | 1 天 | 来源菜单里能从一条来源跳到它参与的那条消息；需要落盘关联（当前无此数据） |
| ~~**P1/P2-启用禁用**~~ | `pi config` 那套启用 / 禁用 | — | **已定：不做**（2026-09-18 用户拍板）。它是 **TUI、没有非交互入口**；三方里只有方案主张「需要时派一个真实终端」，我与会话外咨询都认为那是**伪集成** —— 空壳入口会形成兼容债并让用户误判能力边界。设置里继续只提供安装 / 卸载 / 更新，**不加「打开终端」入口** |
| **G3-其它托管站** | GitLab / Bitbucket 的 PR | 1–1.5 天 | 两家 API 形状不同（不是换域名），方案第一阶段只要求 GitHub，当前界面已如实说「本阶段只接了 GitHub」。**已定：不做**（三方一致，2026-09-18 用户拍板） |
| **非 Git 目录视觉** | 非 Git 目录的界面形态只有数据层与文案覆盖，**没有真实窗口截图** | 0.5 天 | 补一张真实截图（fixture 的 `cwd` 永远是仓库，需要另造状态） |

## 4. 已知限制（**不改就是已知行为，不是待办**）

在决定「做」之前，这些是**有意的取舍**，不要当缺陷修：

- 审查面板是**右栏的宽档位**，不是主内容区（取舍见归档件 §16.4）；**宽度不支持拖动**。
- **没有「按行 / 按块暂存」** —— 已明确列入 **v2 backlog**（不是待办，也不是缺陷）。
- **没有「丢弃工作区改动」** —— 整个 Git 集成里**唯一不可恢复**的操作，**默认不做**。
  方案 §5.1 明确要求不覆盖用户改动；要加它**先要有**「哪些文件会被覆盖」的完整预览与二次确认。
  **「丢弃前自动导出 patch 备份」这个折中已被否**（2026-09-18 二轮咨询，见
  [二轮记录 §2.1](决策记录-二轮外部咨询-2026-09-18.md)）：
  它把「绝对不可恢复」降成「有恢复路径」，**但不是真正可逆** ——
  普通 `git diff` 不含未跟踪文件，`core.autocrlf` 的换行转换与 `submodule`（gitlink）
  都可能让补丁**应用不回去**；更糟的是「已备份」的文案会让用户放松，**反而提高误操作概率**。
  （注：`git diff --binary` 确实能表达二进制改动 —— 所以「二进制没法备份」这个理由**不成立**，
  不要写进文档。真正站不住的是上面三类。）
  真要开先补边界用例：`rename` / 未跟踪文件 / `autocrlf` / `submodule` 四种，
  其测试与确认成本远高于普通 Git action。
- 「已查看」存在渲染端 `localStorage`（本机有效、不跨设备）；
  键含仓库 / 工作树 / 范围 / 两侧内容指纹，**内容一变即失效**。
- 展开上下文**一次最多 300 行**，超出只提示还有多少行。
- 切换分支的阻断判据是「该 `cwd` 有 running 的 runner」；
  **别的进程**（终端里的 git、编辑器）不受我们控制，只能由 git 自己的 lock / 检查兜住。
- 「来源」菜单**不显示**「已读取」与「本轮已参与上下文」—— 证据不足时不显示（方案 §8）。

## 5. 会话切片

| 片 | 内容 | 规模 | 依赖 |
|---|---|---|---|
| **S1** | 补齐低风险取证缺口：非 Git 目录真实截图 + 复审三个已知限制的文案是否与实现一致 | 0.5–1 天 | — |
| **S2** | W2-重绑定：授权 / 相对路径 / 附件 / 来源关系落盘 | 1.5–2 天 | — |
| **S3** | S1-定位消息：来源与消息的关联落盘 + 跳转 | 1 天 | — |
| **S4** | S1-搜索：接入 [04](实施-04-能力自主选择-MCP与Skill.md) 的搜索源 | 1 天 | 04-S5（联网发现适配器） |
| **S5** | G3 其它托管站（GitLab / Bitbucket PR） | 1–1.5 天 | 用户是否需要 |

**S1–S3 互相独立，可任意顺序**；S4 必须等 04；`pi config` 启用 / 禁用**已定不做**（见 §3，不进切片）。

**「丢弃工作区改动」与「按行 / 按块暂存」都不在本计划默认范围内**（三方结论，见 [决策记录 §2.5](决策记录-三方意见对照-2026-09-18.md)）：

- 「按行 / 按块暂存」→ 明确列入 **v2 backlog**（有归属，不再悬空）；
- 「丢弃工作区改动」→ **默认不做**；「patch 备份」的折中已被否（备份 ≠ 可恢复），
  要开需先解决未跟踪 / `autocrlf` / `submodule` 三类边界，并确认「完整预览 + 二次确认」的形态。

## 6. 验收

| 栏 | 标准 |
|---|---|
| 实现 | 只有 `git-actions.ts` 写仓库；新增能力不破坏 §2 三条约束 |
| 自动检查 | `typecheck` / `build` 干净；新增真实仓库写操作有单测（现有基线见 HANDOFF） |
| 真实运行 | `test:live -- gitwrite`（cost 0，**已进 check**）与 `gitreview`（只读保证）不能回归；新功能各补一条断言并**回读主进程真实 git 状态** |
| 视觉验收 | 新状态截图**另存新名**；溢出 ≤ 1px；深浅两套 + 窄窗 |
| 应用与包 | 随 [09](实施-09-交付与验收收尾.md)；`test:packaged` 覆盖新字符串 |
| 剩余限制 | §4 全部逐条保留在文档里，不要静默删除 |

**测试注意**：`gitreview` 用「退出后逐字节比对 `status` / `ls-files -s` / `diff --cached --numstat` / `HEAD`」证明只读，
并做了三条反向验证；改这一层后**必须重跑它**。写操作成功后界面手里的 `expected` 要**就地更新**（用响应里带回的状态），
否则会出现假冲突（这是 G2 这一轮真实抓到的设计问题）。
