# 未完成工程 · 优先级排期 · 2026-09-15（2026-09-17 重整）

> **本文定位**：只剩**未完成**项，按轻重缓急排成一张可执行清单（谁先做、改哪里、怎么算完）。
> **已完成**的任务、缺陷（D1–D41）与逐轮记录见 [2026-09-17 已完成归档](../archive/2026-09-17-已完成归档.md)；
> 当前状态与基线见 [HANDOFF](HANDOFF.md)；工程标准见[工程清单](ENGINEERING-CHECKLIST-2026-09-15.md)；
> 方案细节与根因见[全量审计与收尾方案](AUDIT-2026-09-15-项目全量审计与收尾方案.md)。
> 本文不放流水账，也不重复 HANDOFF 里已经写清的"已取证项还差什么"（那张表在 HANDOFF 第三节）。

## 优先级定义

| 级别 | 判断标准 | 处理方式 |
|---|---|---|
| **P0** | 会产生**错误结果**、**进程/目录残留**，或**阻塞发布结论** | 先修，再谈验收 |
| **P1** | 主链路能力已有实现但**零真实验收**，或功能闭环缺最后一环 | 紧接着做，与 P0 同批 |
| **P2** | 体验、视觉、文案、边界补充；不影响正确性 | 批量补齐 |
| **P3** | 需要证书、目标系统、上游接口或产品决策，代码侧无事可做 | 不排期，等条件 |

---

## P0 · 只剩发布门槛

| # | 事项 | 类型 | 改动点 / 流程 | 规模 |
|---|---|---|---|---|
| P0-8 | **L01 发布门槛**：便携数据**副本**升级验证 + 最终源码安装包 / 便携 ZIP / SHA256 | 验收 | `npm run dist:dir` / `dist` / `dist:portable-fast` + [RELEASING](RELEASING.md) 流程；`release/砚数据/` 只读，只用副本 | 0.5 天 |

**用户已明确要求：放到最后做**（它依赖所有功能合入）。它同时是 N21-2/N21-3、L02/L04/L05、N11、N16、N03 等项
「应用与包」那一栏的共同缺口 —— 做完这一项，那些行才算六栏齐备。

---

## P1 · 功能闭环（两块）

### P1a · 上下文管理器 N21：阶段 4 多阶段与归档

方案（唯一真源）：[把自动压缩收进上下文工具](../design/方案-上下文工具内的自动压缩-2026-09-15.md)。
职责边界：**砚** `ContextPolicy / ArchiveStore / Settings / UI State`（何时做什么）｜
**pi Extension** `ContextTransformer / CompactionAdapter / RecallTool`（具体怎么改送给模型的消息）。
阶段 0–3（N21-0～N21-3）已完成，证据见归档。

| 编号 | 阶段 | 优先级 | 状态 | 交付物 | 验收 | 规模 |
|---|---|---|---|---|---|---|
| **N21-4** | 多阶段与归档 | P1 | 待办（依赖 N21-3 ✅；**开工契约已定在方案 §12**） | Extension 内：`context` 钩子做 tool sweep / episode fold / recall 注入；`session_before_compact` 接管结构化压缩；archive 元数据 + `recall` 工具 | 方案 §12.11 的 10 条（原子单元切割 / 可回收量不足时跳过 / 三阶段独立 Rearm+Cooldown / 两个 schema / 禁止递归摘要 / Recall 预算与生命周期 / 20+ 回合压力测试）；只有 `recallable = agent` 时才写「模型可回溯」 | 3–5 天 |

**开工前必须先定完 §12.12 的 P0 五项**：原子上下文单元（`recentTail` 切割规则）、`EpisodeState` schema、
`CodingState` schema、禁止递归摘要（含 `sourceRange` 判据）、Recall 生命周期（ttl + 预算 + 审计），
外加降级路径（每个变换失败都要退回 pi 原生行为）。

### P1c · 上下文状态化压缩（按《长上下文状态化压缩参考方案》对齐，2026-09-17 立）

> 参考方案原文：[design/参考-长上下文状态化压缩（外部方案·原文）](../design/参考-长上下文状态化压缩（外部方案·原文）.md)；
> 对齐结论（逐条采纳/修订/不采纳 + pi 能力核实 + 数值取舍）在
> [方案-上下文工具内的自动压缩 §13](../design/方案-上下文工具内的自动压缩-2026-09-15.md#13-参考方案对齐state-first-compaction--trace-as-state2026-09-17)。
> 与 **N21-4** 的关系：N21-4 是执行层（扩展骨架 + sweep/fold/archive/recall），本节的 N21-5～N21-10 是
> **状态层与顺序层与安全规则**（状态怎么存、放在哪、怎么更新、怎么对照验证）。两者共用同一套 schema
> （§12.6 / §12.8 + §13.4），不要另起一套。

| 编号 | 内容 | 优先级 | 状态 | 依赖 | 完成标准与证据 | 规模 |
|---|---|---|---|---|---|---|
| **N21-5** | **Task State 与 State-First 注入顺序**：`TaskState`（= §12.8 `CodingState` + 参考方案字段：`commandsRun`/`testsRun`/`symbolsTouched`/`assumptions`/`hypothesis`）落成状态文件；`StateEntry.status`（active/resolved/superseded）+ `StateFact.source·confidence` provenance；增量 delta 更新（Existing State + Delta）。扩展的 `context` 钩子把 `<TASK_STATE>` 插到**历史之前**（`SYSTEM → TASK_STATE → HISTORICAL_CONTEXT → RECENT_RAW → QUERY`） | P1 | 待办（等 N21-4 骨架） | N21-4；用户拍板 ①（状态文件位置/可见性） | 真实回合里断言"发给模型的消息顺序"（`context` 钩子自报 + 探针读状态文件）；状态文件原子写、可回滚、会话删除时清理；旧决策被 `superseded` 后不再出现在注入里 | 1–2 天 |
| **N21-6** | **`session_before_compact` 结构化压缩（两段式）**：按核实过的 payload（`messagesToSummarize` / `turnPrefixMessages` / `previousSummary` / `fileOps` / `tokensBefore` / `firstKeptEntryId`）生成 `<TASK_STATE>` + `<HISTORICAL_CONTEXT>`；保留 pi 给的 `firstKeptEntryId` 切割点（我们只换摘要文本）；采纳参考方案 §6 的 **State Refresh** 档（工作集 60% 就刷新状态，不必等到压缩） | P1 | 待办 | N21-4 | 压缩后六类字段（任务/decisions/constraints/failed/unresolved/nextActions）逐一非空；`previousSummary` 只作参考、**不得**摘要摘要（§12.7 判据：`sourceRange` 必须指回原始条目） | 1–2 天 |
| **N21-7** | **有效窗口与阈值的可配置化 + 模型级 override**：把 `workingSetCap` / `windowRatio` / `responseReserve*` / `safetyMargin*` / `triggerRatios` / `emergencyRatio` 从"只有代码默认值 + `YAN_CONTEXT_POLICY`"升级成设置面板可改 + 按模型覆盖；同时把参考方案的 300k/0.75/五档作为**一组可选项**提供（不是新默认） | P2 | 待办 | 无（已实现部分由 N21-3 打底） | 设置面板改一个值 → `contextbudget` 探针里"界面数 = 主进程数"仍成立；模型级 override 在切换模型后生效且可解释 | 0.5–1 天 |
| **N21-8** | **Deep Context Mode（Trace-as-State 双遍）**：Pass 1 用砚侧独立 pi 进程（`title.ts` 已验证的模式）生成 trace/working state；Pass 2 由 `context` 钩子把它作为**前置状态**注入（`[T, X, Q]`）；触发条件按参考方案 §14（>200k、跨文件/全仓调试、档位 High/Max…），**默认关闭**、可手动开 | P2 | 待办（**前置调查未做**） | N21-5；用户拍板 ①；前置调查两项 | 前置：① `context` 钩子注入额外 system 消息在真实回合里生效；② 多一次调用的成本/延迟与 KV cache 影响实测。之后：同一任务的 A/B（C 与 D 两策略）对比，且失败时能退回单遍 | 2–3 天 |
| **N21-9** | **A/B 基准与指标**：四策略对照（A 原始长上下文 / B 传统摘要 / C State-First / D State-First + Trace）；指标含任务成功率、单测通过率、错误修改率、重复读文件次数、回滚次数、token、延迟、压缩频率、状态准确率、**Lost Constraint Rate（丢失约束率）** | P2 | 待办 | N21-5/N21-6（C 就绪） | 用合成任务集 + `YAN_CONTEXT_POLICY` 把工作集压到小额度跑（成本可控），再补一次真实长会话；`Lost Constraint Rate` 要能在压缩后逐条核对用户约束 | 1–2 天 |

> **N21-10 已完成**（2026-09-17，安全规则补三条）：`resources/pi-extensions/context-safety.js`
> + 42 条单测（`npm run test:unit` 951/951）。N21-4 开工时**直接调它**，不要另写一套切割/保护逻辑；
> 明细见 [已完成归档](../archive/2026-09-17-已完成归档.md) §1.7。

**前置调查（放在 N21-8 开工前，不单独占排期）**

```text
① pi 0.85.1 扩展能力 —— 本轮已核实（见 §13.3）：
   · context 钩子存在且返回值即生效（emitContext：返回 .messages 就整体替换）
   · session_before_compact 的 payload 字段与参考方案一致
   · 扩展侧**没有**"跑一次推理"的 API → Pass 1 必须由砚侧起独立 pi 进程（title.ts 先例）
② 仍需实测：注入额外 system 消息后的真实回合、多一次调用的 token/延迟、KV cache 影响
```

---

### P1d · 已关闭缺陷的回归网（2026-09-17 晚新增，都已修并验证）

用户 2026-09-17 报的三条（切会话历史丢失 / 看不到本次用时 / 推理不跟随界面语言）查出五个缺陷，
**全部已修**，明细与证据见 [HANDOFF 的本轮小节](HANDOFF.md)：

| 编号 | 一句话 | 回归网（下次改这块必须跑） |
|---|---|---|
| D37 | 切语言重建 pi 实例会把界面停在新会话上（历史当场被清空） | `test:live -- language`（断言不重建）、`historyswitch` 第 3 节 |
| D38 | 切会话被 pi 的上下文视图 / 旧 `sync` / 运行时缓存投影清空或换掉 | `test:live -- historyswitch`（cost 0，已进 check 候选） |
| D39 | 语言要求做成了启动参数，切语言只能靠重建、已有会话永远拿不到 | `test:live -- language` + 扩展单测 |
| D40 | 同一句要求挂在系统提示末尾时模型不服从（位置问题） | 单测 + `language` 第 2 节；摘掉 payload 注入必须变红 |
| D41 | `e2e` 探针内部等 150s > case budget 90s，被杀死在打印前，报"应用可能启动失败" | `test:live -- e2e`（已补 `budget: 200000`） |

**剩下的已知失败**：`todos`（陈旧失败，在 check 列表里，按产品边界保持现状）、`subagent`（在 check 列表里，
免费模型当天返回**空文本**：`status=done` / `error=null`）、`compactionstatus`（**不在** check 列表，
手动场景，且前提过期：pi 0.85.1 不再按 `reserveTokens` 在回合结束自动压；用改动前的实现重跑同样失败，
app 侧压缩链路由 `contexttakeover` 覆盖并通过）。

后者待办：要么改成用 `YAN_CONTEXT_POLICY` 驱动（把"回合结束自动压"换成"砚按工作集发起"，
与 `contexttakeover` 合并成一条），要么从手动清单里降级说明。

---

## P2 · 体验与视觉补齐（只剩新功能与已取证项的尾巴）

| # | 事项 | 说明 | 规模 |
|---|---|---|---|
| P2-7 | **N01 拖拽排序**（可选增强） | 分组与项目顺序拖拽 + 落盘；与「前五项折叠」、搜索态互斥。**代码里没有 reorder/drag 处理** —— 当新功能做，不要再列成"验收未做" | 0.5–1 天 |
| P2-尾巴 | **已取证项的剩余限制** | N12（等待输入/失败/未读的真实后台证据、退出变体、孤儿进程观测）、L03（模型失败/超时恢复）、N04（真实 thinking 的长推理流截图）、N10（光带运行中截图）、N18（逐命令真实执行反馈、技能真实发现）、N20（引导点击回路只有探针）、N05（「每个项目最后一个会话」持久化）、N09（`restoreQueue` 失败分支只可审阅）、L04（真实账号登录）、L05（外部进程改动看不到）——**逐条清单与「差什么」见 [HANDOFF 第三节](HANDOFF.md)** | 见各项 |

## P3 · 外部条件阻塞（不排期）

| 事项 | 缺什么 | 现在能做的 |
|---|---|---|
| 代码签名 | 开发者证书 | 只发布未签名产物并如实标注 |
| macOS / Linux 包 | 对应构建环境 / CI | 共享类型与路径抽象不写死 Windows |
| 扩展 `registerShortcut` | pi 0.85.1 RPC 无枚举/执行接口 | 等上游；不做静态假映射 |
| `image` 场景视觉验收 | 视觉模型 | 前端链路已通过，标注"模型能力边界" |
| Yan 自有账号 / 跨设备同步 | 产品与服务端 | 本地档案保持"未接入"真实状态 |
| Command Code 月度额度 | 账户权威字段 | 继续标注为估算 |

**不排期且不要重做**：任务面板整体改造、记忆模块、`get_tree` 浏览链路 —— 均为已确认移除的产品边界。

---

## 建议顺序

```text
N21-4（执行层骨架 · 3–5 天 · 先定 §12 的 P0 五项）
   ↓
N21-5（Task State 与 State-First 顺序）→ N21-6（两段式结构化压缩）
   ↓
N21-7（阈值可配置化 · 随时可插）→ N21-9（A/B 基准 · 等 C 就绪）→ N21-8（Deep Context · 先两项前置调查）
   ↓
P0-8（发布门槛 · 用户要求放最后）→ P2-7（拖拽排序 · 新功能）
```

改 `src/` 后跑 live 前必须先 `npm run build`；跑 Electron 前要 `env -u ELECTRON_RUN_AS_NODE`；
`release/砚数据/` 全程只读，升级验证只用副本。
